# Decisions and known deviations

Two tables. The first is every decision that affects a result, with the reason
it was made. The second is every place this code knowingly disagrees with a
reference or with one of its own docs, with the reason it is not a bug.

Both are load-bearing. Tests cite rows here to justify what they assert, so a
row that changes has to be traced into the tests that lean on it. Never silently
reverse a decision; add a new dated row instead.

The running session narrative lives in PROGRESS.md, which is local and not part
of the repo. This file is the part that a reader of the code needs.

## Physics decisions log

Decisions that affect results, with reasoning. Never silently reverse one of
these; add a new entry instead.

| Date | Decision | Reason |
|---|---|---|
| init | n_i = 1.0e10 cm^-3 at 300K | matches textbook worked examples used as analytic targets. See docs/06-constants.md |
| init | C_0 = n_i for de Mari scaling | cleanest Poisson form. Revisit if Phase 5 conditioning degrades |
| init | Boltzmann statistics through Phase 4 | Fermi-Dirac deferred to Phase 5 where degenerate S/D makes it necessary |
| init | Structured tensor mesh in Phase 4 | avoids obtuse triangle problem entirely for rectangular geometry |
| init | Steady state only | transient out of scope. C-V by small-signal AC, not time stepping |
| 2026-08-21 | n_i(300) anchored to 1.0e10, temperature dependence from physics | 1.0e10 cannot be derived from the Nc, Nv and Eg in docs/06-constants.md, those give 1.0757e10. The logged decision wins for the 300 K value and the physics supplies only the T scaling. n_i^2/(Nc Nv exp(-Eg/V_T)) is then exactly T independent, which is tested |
| 2026-08-21 | Eg from the Varshni formula, not the rounded table value | the doc gives both, they differ by 8.1e-5 eV. The formula is the definition and the table entry is it rounded |
| 2026-08-21 | Bernoulli derivative series widened to abs(x) <= 0.1 with terms to x^7 | the doc's recipe (x^3 series, cutoff 1e-4) has 1.8e-8 relative error just outside the cutoff, which fails Phase 0's own 1e-13 criterion by five orders of magnitude. Measured, see the Broke entry below |
| 2026-08-21 | B(x) uses -x*exp(-x)/expm1(-x) for x > 0, no cutoff at 80 | never overflows, underflows to 0.0 on its own past x = 745, and keeps the 290 decades of representable values that "return 0.0" would discard |
| 2026-08-21 | Field.to_scaled on an already scaled field raises rather than no-op | calling it means the caller has lost track of state, which is the bug worth surfacing |
| 2026-08-21 | solve/linear.py always factorizes fresh with COLAMD | no part of a scipy splu factorization is reusable. Measured, see the Broke entry below |
| 2026-08-21 | Graded mesh spacing is monotone per side, not globally | globally monotone is impossible for an interior refinement, the spacing has to fall to h_min and rise again |
| 2026-08-21 | Poisson carries fixed phi_n and phi_p, not one common phi | reverse bias splits the two levels by exactly the applied bias. One common level with a step at the junction drives n to 1e26 cm^-3 there and shrinks the depletion width threefold. Measured |
| 2026-08-21 | Newton residual criterion is relative to the initial residual | an absolute threshold is not scale free. The Poisson residual scales with the doping and so does its roundoff floor, so a fixed 1e-10 that works at 1e16 cannot be met at 1e18 |
| 2026-08-21 | Ohmic contacts by row replacement, giving up matrix symmetry | costs nothing with a direct solver and keeps the assembly obvious |
| 2026-08-21 | Step doping profile is right continuous at the junction | arbitrary, but it has to be decided somewhere, and an abrupt junction is an idealisation anyway |
| 2026-08-21 | Equilibrium minority carrier from n*p = 1, not the quadratic formula | the quadratic formula loses twelve digits computing the minority carrier at 1e16. The reciprocal makes mass action exact rather than merely close |
| 2026-08-21 | device/doping.py and device/equilibrium.py added to the layout | docs/03-architecture.md names neither. Profiles do not belong inside builder.py, and the equilibrium driver has to live outside solve/ to keep that package free of semiconductor knowledge |
| 2026-08-21 | Gummel freezes the SRH denominator rather than using the exact tangent | Writing R as c*n - g with c = p/D and g = n_i^2/D keeps both coefficients non-negative. The continuity matrix is an M-matrix, so a non-negative right hand side means the solved density cannot go negative. The exact tangent dR/dn has no such guarantee. Both forms are exact at the current iterate, so the converged fixed point solves the true equation either way. The exact derivatives are implemented and tested, for Phase 3 |
| 2026-08-21 | Terminal current comes from the continuity residual at the contact node, not the adjacent edge flux | The residual at a contact is exactly the current that has to be injected there. Written that way, the recombination in the contact half cell cancels between the two carriers, and the terminal currents sum to zero identically rather than approximately |
| 2026-08-21 | Dirichlet eliminates the column as well as the row | Row replacement alone leaves the pinned unknown coupled into its neighbours, so the factorization mixes it with the rest of the solution. Measured: a density pinned to +1e-6 came back as -1.02e-6. See the Broke entry |
| 2026-08-21 | Contact densities are imposed on the solved profile, not accumulated through it | old + update cancels when the two are far apart, and on the first forward biased cycle they are thirteen decades apart. Imposing a value that was known before the solve is what a Dirichlet condition means; it is not clamping, and no interior node is touched |
| 2026-08-21 | newton_solve takes an explicit residual scale for warm starts | A threshold relative to the initial residual is unreachable when the solve starts at the answer, which is what every Gummel cycle after the first does. The Poisson block passes the doping charge in the largest dual cell, which does not depend on the starting iterate |
| 2026-08-21 | Density convergence measured as max abs(dn) / (n + n_i) | A pure relative change is dominated by nodes where the density is 1e-15 and physically irrelevant; an absolute change is dominated by the majority carrier. The floor at n_i is 1 in scaled units and says that a carrier below the intrinsic density carries no charge worth converging |
| 2026-08-21 | The Scharfetter lifetime is evaluated on abs(net doping) | It wants the total Na + Nd and only the net is available from a composed profile. The two agree everywhere except in compensated material, and nothing here is compensated yet |
| 2026-08-21 | The ideality crossover is demonstrated on a 1e18 device, not the 1e16 one | At 1e16 with the documented lifetimes the diode is diffusion limited and its ideality is 1 above 50 mV, which is correct rather than a shortfall. Depletion recombination has to dominate somewhere for n = 2 to exist, and raising the doping does that by cutting minority injection and raising the recombination rate at once. Nothing else changes between the two |
| 2026-08-21 | newton_solve gives up once the residual is frozen to the last bit and the update is already inside tolerance | Both criteria still have to pass and the result is still converged=False, so the outcome is unchanged and only the iteration count moves. The check runs after the convergence test, so a warm start that arrives with a frozen residual and a zero update is still reported as the success it is. A frozen residual under a still moving iterate is deliberately not caught: that is a solver taking real steps that happen not to help, which is a different failure and gets the whole budget. Verified bit for bit inert on psi, n, p and the terminal currents at seven biases |
| 2026-08-21 | extract/params.py and device/transport.py and device/state.py added to the layout | docs/03-architecture.md names params.py and puts ideality extraction in it. transport.py is the Gummel wiring, which cannot live in solve/ without breaking the module boundary. state.py holds DeviceState, which both equilibrium.py and transport.py return |
| 2026-08-22 | The coupled dF_psi/dpsi carries no Boltzmann charge term | In Phase 1 n and p are functions of psi, so the diagonal picks up (n + p)*volume, which is what makes that matrix an M-matrix. Coupled, they are separate unknowns and the same physics lives in dF_psi/dn = +volume and dF_psi/dp = -volume. Carrying it in both places is the most natural way to get this wrong, because the Phase 1 Jacobian is sitting right there to copy from, and Newton would still converge, slower, to the right answer. The block verification catches it and nothing else does |
| 2026-08-22 | The coupled Jacobian uses the exact SRH tangent, not the Gummel frozen denominator | The frozen form exists to keep the continuity matrix an M-matrix so a solved density cannot go negative. The coupled matrix is not an M-matrix under any linearization, so that guarantee is not available to give up. Positivity comes from damping psi instead. Measured: the two give residual histories identical to three significant figures on a 1e16 diode at 0.8 and 1.0 V, so the exact tangent is not bought for convergence, it is bought for being right |
| 2026-08-22 | The coupled system is row scaled by equation family before the solve | One residual threshold cannot serve a charge and a current. The Poisson rows are order N*volume and the continuity rows are order (D/h)*n, six decades apart on a 1e16 device, so a single max abs(F) is set by the larger and declares Poisson converged a million times above its own floor. Each family is divided by the largest single term that goes into it. It is a diagonal left preconditioner, so it changes no answer, and it is applied outside the assembly so the Jacobian the block verification checks is the unweighted one |
| 2026-08-22 | The coupled update is measured per family, absolutely on psi and relative on the densities | max abs(dx) over the raw vector cannot fall below 1e-9 when n is 1e6 in scaled units, so no threshold suits both. docs/02-numerics.md already asks for the carrier change as max abs(dn)/(n + n_i). The floor at n_i matters as much as the ratio: without it the measure is dominated by nodes where the density is 1e-15 and carries no charge |
| 2026-08-22 | Only psi is damped in the coupled Newton, and the psi sub-vector is scaled rather than clipped entry by entry | Both docs/02-numerics.md and docs/05-pitfalls.md prescribe capping psi and taking the full n and p updates. One factor over the whole vector would be set entirely by the density update and would freeze psi. Within psi the direction is preserved, so the rotation is only between psi and the densities and only while the cap is active |
| 2026-08-22 | physics/bernoulli.py grew a complex branch, in production rather than in the test tree | phases/PHASE-3.md makes complex step verification a permanent CI requirement, so a residual that survives a complex argument is a property the code has to have, not test scaffolding. The real path is untouched: one dtype test per call and every array on it is still float64 |
| 2026-08-22 | solve/newton.py takes a limit callable and an update_norm callable rather than learning about components | Both are the same shape of problem: a scalar rule that cannot express a system whose unknowns differ by decades. Injecting them keeps solve/ free of semiconductor knowledge, which the import graph test enforces |
| 2026-08-22 | Phase 3 scope item 8, symbolic factorization reuse, is dropped rather than attempted | Phase 0 already measured it. scipy exposes no symbolic and numeric split, and the standard workaround gives 6.1x fill and a 46x slowdown in 2D. Nothing has changed since. The interface still lets a UMFPACK or KLU backend deliver it later |
| 2026-08-22 | The row scale is measured at every iterate, not frozen at the starting guess | "Does not depend on the starting iterate" means it must not depend on how converged the start is. It does not mean freezing it. The terms a residual is built from are a property of the state, and on a forward biased junction they grow with the injected density: 28 times between guess and answer on a 1e16 diode at 1 V, 660000 times on a 1e20 / 1e14 one. The threshold itself never moves, at residual_rtol against a scale of one; what is re-measured is the question |
| 2026-08-22 | The Poisson term scale counts the carriers, not only the doping | The residual carries -(p - n + N)*volume, and on intrinsic material that sum is exactly zero while n*volume and p*volume are a dual cell each. A scale built from the doping alone is zero there and every row divides by it. An undoped bar returned a residual of nan and a message blaming the LU factorization for being singular |
| 2026-08-22 | Arora mobility is opt in, and the constant model stays the default | Every Phase 1 and 2 number was taken with the constant model, and Arora moves the current by five percent even at doping too low for it to be doing anything, because its N -> 0 limit is 1340 rather than the tabulated 1417. Making it the default would silently invalidate every measurement already in this file |
| 2026-08-22 | Mobility is averaged from nodes to edges arithmetically | Scharfetter-Gummel derives its edge flux assuming the coefficients are constant along the edge, so one value per edge is what the scheme asks for and the only question is which. The mean is the honest reading of a quantity that is genuinely varying, and on a mesh graded to a junction the two endpoints sit in nearly the same doping anyway |
| 2026-08-22 | Auger is opt in as well, and off by default | It changes nothing measurable below high injection, and the same argument about invalidating recorded numbers applies. Its exact tangent is also not sign definite, unlike the SRH one, so turning it on by default would quietly remove a property the Gummel path documents relying on |
| 2026-08-27 | A carrier flux crosses only the semiconductor share of an edge's face, carried on the geometry beside the permittivity | The zero charge volume in an insulator kills the terms the equations integrate, and a flux is not one of those. Without a separate face, the edge running from the silicon surface into the oxide still moves electrons into a node whose density is pinned at zero, which is a perfect sink bolted to the channel of every MOSFET. The share is the same area weighting the permittivity gets, so an edge along the interface offers half its face, and on a single material device it is exactly 1.0 and every recorded number is unchanged |
| 2026-08-27 | The coupled Newton path takes its mesh, its charge volume and its edge geometry from the device rather than assuming a line | Phase 4's current continuity criterion could not run at all while no device had solved transport on a Mesh2D. Nothing about box integration is 1D; only the wiring was. The Gummel path is still 1D, slices edges contiguously and says so |
| 2026-08-27 | An insulator node's two continuity rows are pinned at zero density, and its Poisson row is left alone | With no charge volume and no carrier face those rows read 0 = 0, and a singular matrix is not something to discover inside a factorization. Zero is also the true statement: an insulator holds no free carriers. The Poisson row is a Laplacian and wants no help |
| 2026-08-27 | A terminal current is the residual summed over every node the contact covers | A plate is one piece of metal, so the current into it is the current into all of it. A point contact is that sum over one node, which is why there is no 1D branch. Reading one node of a plate leaves most of the current unaccounted for and the terminal currents stop summing to zero |
| 2026-08-27 | current_densities divides the edge flux back by the carrier face, and reports exactly zero where that face is zero | The flux kernels multiply by the face because that is what a continuity equation wants. In 1D the face is exactly 1.0, so the quantity was a density by accident and the division is exact. In 2D it is not, and a number named a density has to be one |
| 2026-08-27 | The README C-V figure is benchmark 4, the 5 nm capacitor, not the 10 nm device the analytic tests use | Phase 4's definition of done asks for the curve overlaid on DEVSIM's, and golden data exists for 5 nm and 20 nm only. The analytic table stays on the 10 nm stack, where the doping sweep lives. Both are the same code path |

| 2026-08-27 | A doping profile is called with a Coordinates carrying x and, on a grid, y, and a bare array still means x | Phase 5 is the first thing in this project whose doping varies with depth. Making the second axis a new argument would have changed every profile ever written; making it a container that a bare array falls into changes none of them. Verified bit for bit on all five benchmark devices, so DEVSIM regressions 1 to 5 measure the same numbers they did before |
| 2026-08-27 | The source and drain implant is separable, an erfc edge along x times a Gaussian in depth | A real two dimensional diffusion is not separable, and the difference lives in the corner where the lateral and vertical fronts meet. What the device is actually sensitive to is where the junction sits: the depth under the contact and the encroachment under the gate edge, and both of those are on an axis where the profile is exact. Being separable is also what makes them closed form, so a test can assert the junction depth and the lateral diffusion against the numbers asked for rather than against a previous run |
| 2026-08-27 | The gate electrode covers the channel span exactly and the oxide over the source and drain has no electrode on it | This is the ideal structure: no gate overlap, so no overlap capacitance and no gate induced drain leakage. Both are real effects and both would be additions to the geometry rather than to the physics, so leaving them out costs nothing that this phase is measuring. The uncovered oxide takes the natural condition, dpsi/dn = 0 |
| 2026-08-27 | The source and drain contacts stop short of the gate mask edge, by contact_length being less than sd_length | A contact pins psi, n and p. One running up to the metallurgical junction pins the junction itself, and the built in potential stops being something the solver works out. The builder refuses the geometry rather than solving it |
| 2026-08-27 | The NMOS carries no halo implant, no lightly doped drain and no fixed interface charge | Every one of the three exists to move a threshold voltage or to soften a field. On a phase whose success criterion is that short channel effects emerge without fitting, adding a feature whose purpose is to counteract one of them would make the result unreadable |

| 2026-08-28 | Caughey-Thomas reads the potential drop along the edge, not the magnitude of a nodal field vector | The field a carrier crossing an edge sees is the component along that edge, which on a box integration mesh is exactly the X the Scharfetter-Gummel argument already carries. Reconstructing a vector at a node and taking its length instead gives one field to every edge leaving that node, and on a mesh graded from 2 nm to 200 nm at a junction those edges do not see the same field. It also gives the mobility a value at a node, where a field does not have one |
| 2026-08-28 | Field dependent mobility is opt in through field_dependent, and stays off by default | The same argument that keeps Arora and Auger opt in. Every number recorded in this file before today was taken without it, and the None branch in the Jacobian means a solve without it does exactly the arithmetic it did before the model existed rather than adding a zero |
| 2026-08-28 | The absolute value inside Caughey-Thomas is written as sqrt(X*X), and only on the complex branch | abs() of a complex number is its modulus, which is not holomorphic, so a residual containing one returns an imaginary part of zero and the block verification would report every field dependent term as missing rather than as wrong. Same shape as the complex branch in physics/bernoulli.py and there for the same reason. The real path stays np.abs, because squaring first would underflow to zero and overflow to infinity at the ends of the range |
| 2026-08-28 | dD/dX enters the Jacobian as the flux over D times the tangent, written out rather than as D times dlnD/dX | The logarithm form is shorter and divides by a diffusivity the model is allowed to drive small. The two flux derivative blocks each gain one term and no block gains a new sparsity pattern, because the drop across an edge depends on the same two nodes the flux already did. Measured: mutating either term out fails the complex step block verification at the perturbed state |
| 2026-08-28 | The Gummel blocks lag the field dependent diffusivity at the potential of the cycle rather than carrying its tangent | A Gummel block solves one continuity equation with psi held fixed, so within the block the field is a constant and the diffusivity with it, which is what lagging a coefficient means and what the cycle already does to everything else it holds. At the fixed point the frozen diffusivity is the converged one, so the decoupled path solves the same equations the coupled one does. What is given up is a rate of convergence, not an answer |

| 2026-08-29 | Surface mobility is the enhanced Lombardi, with DEVSIM's own parameters | docs/01-physics.md names Lombardi and gives no parameters, and docs/06-constants.md has none either, so they had to come from somewhere. DEVSIM ships the Darwish variant in its Klaassen.py and DEVSIM is what tier 4 regresses against, so matching the model the reference actually runs is what gives benchmarks 6 to 9 a chance of agreeing. It is the 1988 model with the roughness exponent made a function of carrier density rather than fixed at 2. A test pins all eight parameters per carrier against the reference's source |
| 2026-08-29 | E_perp is the field normal to the interface, taken as the y derivative on the tensor mesh, and it is a magnitude averaged from the signed nodal field | The interface in every device here is a horizontal line on a tensor product mesh, so the normal direction is y and DEVSIM's general triangle machinery buys nothing. The absolute value is taken after the averaging, not before: where the field reverses across a node the field at that node really is near zero, and averaging magnitudes would report the average of two large ones at exactly the places a device has a potential well. A boundary row averages the one edge it has, not two |
| 2026-08-29 | The surface correction is frozen inside a Newton solve and made a fixed point by an outer loop | For a horizontal channel edge the normal field lives on the vertical edges above and below its endpoints rather than on the edge itself, so carrying its tangent would widen the Jacobian stencil past the edge based pattern every assembly in discretize/ is built on. Frozen, the residual and the Jacobian come from the same mobility, so they agree exactly and the nine block complex step verification is untouched, which phases/PHASE-3.md makes a permanent CI requirement. At the fixed point the mobility the solve used is the one the answer implies, so the converged state solves the true equations. Measured: re-solving from the answer with the mobility it implies costs zero Newton steps and returns psi, n and p bit for bit identical. Two or three sweeps on a 1 um NMOS |
| 2026-08-29 | An exhausted surface sweep budget is reported as not converged, even though the last Newton solve converged | What has not converged is the fixed point between the mobility and the state it is read from, and the inner result would claim a convergence the solve does not have. The reported iteration count and both histories are summed over the sweeps, so the residual history carries one sawtooth per refresh, which is the honest picture of the method rather than a defect in it |
| 2026-08-29 | The surface correction is applied at semiconductor nodes only, and the doping it reads is floored at n_i | An insulator has no surface mobility, and it has exactly zero doping, which the roughness exponent raises to a negative power and returns an infinity for. Not harmless if left: an edge from the interface into the oxide averages its two ends, so the infinity reaches the interface row, which is the channel. The floor is the separate half of the same problem and says that below the intrinsic density there are no scattering centres left to count |
| 2026-08-29 | A cold solve with velocity saturation is walked up to it through the low field models | The Poisson guess is not in the basin of a Caughey-Thomas solve on a MOSFET. Measured on a 1 um NMOS: 60 iterations with 55 of them against the step limiter and no convergence, against six from the low field answer and zero at the off state, where the field dependence changes nothing because no current flows. It is continuation in the model rather than in the bias and it is the same shape as the Gummel prelude beside it. Surface scattering needs no prelude: it is outside the Jacobian and only scales the mobility by a few, and the same device cold starts through it in six steps |
| 2026-08-29 | A current is read with the diffusivity the answer implies, not the one the models were built with | For a field dependent mobility this was already true and said so. For a surface corrected one it was false: the models as built carry the uncorrected bulk mobility, and the correction only ever existed inside the solver. Measured on an inverted 1 um NMOS, the terminal currents summed to 7.4e-6 of the drain current with the stale diffusivity against 7.6e-7 with the right one |
| 2026-08-29 | A surface corrected solve's terminal current sum is bounded by residual_rtol rather than by machine precision | The last sweep of a fixed point starts from the previous sweep's answer, so Newton stops the moment it crosses the threshold instead of running into its quadratic tail, and the residual it stops at is the threshold. Measured on an inverted 1 um NMOS: 7.6e-7 of the drain current at the 1e-10 default, 1.2e-10 at residual_rtol=1e-12, and no dependence at all on surface_rtol. Not a conservation error and not the fixed point; the diode's 1e-8 gate is a cold solve's luck rather than a promise |
| 2026-09-01 | Fermi-Dirac enters as a degeneracy factor multiplying the Boltzmann form, not as a replacement for it | Writing n = Nc F_{1/2}(eta) directly makes the intrinsic density come out as sqrt(Nc Nv) exp(-Eg/2V_T) = 1.0757e10, and this project anchors n_i at 1.0e10. Row 1 of the deviations table below records that the two are 7.6 percent apart and cannot both be primary, and it says the inconsistency bites the first time something computes a Fermi level from Nc. This is that moment. So the relation stays n = n_i exp(psi - phi_n) with a factor gamma = n/(Nc exp(eta)) on it, gamma is exactly 1.0 at zero density rather than 1.0 to rounding, and every nondegenerate result in the project is unchanged bit for bit. The n_i inconsistency stays exactly where it was instead of being smeared into the degenerate correction |
| 2026-09-01 | The generalized Einstein ratio is the term by term derivative of the same Joyce-Dixon series, not a separate approximation | D/(mu V_T) = u * deta/du exactly, so differentiating eta = ln(u) + sum A_k u^k gives 1 + sum k A_k u^k with no new coefficients. The alternative is evaluating 2 F_{1/2}/F_{-1/2} by quadrature at a numerically inverted eta, which costs two integrals per node per assembly and, worse, is a different approximation from the one the density used. Sharing the series makes the ratio and the inversion consistent by construction: the tangent of one is the other, which is what a Jacobian needs. Measured against Brent inversion of the integral, the closed form is 2.2e-4 relative at u = 4 and 4.4e-3 at u = 8 |
| 2026-09-01 | Joyce-Dixon refuses above n/Nc = 8 rather than extrapolating | The series is a fit and it eventually turns over. Measured against Brent inversion, relative error in n then in D/(mu V_T): 4.4e-5 and 2.2e-4 at u = 4, 9.0e-4 and 4.4e-3 at u = 8, 2.3e-3 and 1.1e-2 at u = 10, 4.1e-2 and 1.9e-1 at u = 20, and past u = 20 the Einstein ratio goes negative, which is a diffusivity pointing the wrong way. Eight is the last point where both are inside the 1 percent docs/04-validation.md asks for. It is 2.29e20 cm^-3, which clears the 1e20 source and drain with room, and a device that really is more degenerate than that should get a rational approximation rather than a silent extrapolation |
| 2026-09-01 | F_{1/2} is evaluated by fixed order Gauss-Legendre on x = t^2, not by adaptive quadrature | The substitution removes the square root branch point at the origin that would otherwise cost the rule most of its order, and the panel split at t = sqrt(eta) puts the shoulder of the occupancy on a panel boundary instead of in the middle of one. 96 nodes per panel returns F_{1/2} to 5e-15 relative against both the alternating series and scipy's adaptive quadrature, over eta in [-40, 40]. Fixed order rather than adaptive because the result has to be the same bits every time it is called at the same eta: an adaptive rule changes its subdivision under a perturbation, and a Jacobian entry derived from one is compared against complex step at 1e-10 |
| 2026-09-01 | Degeneracy will enter transport as an effective potential inside the Bernoulli argument, not as a generalized Einstein ratio multiplying D | The two are the same physics written two ways and doing both would double count. Writing n = n_i exp(psi - phi_n) gamma and defining psi_eff = psi + V_T ln(gamma) makes n exactly Boltzmann in psi_eff, so the flux is q mu (n dpsi_eff/dx - V_T dn/dx), which is the ordinary drift-diffusion form with D = mu V_T and the Scharfetter-Gummel exponential fit exactly valid in psi_eff. The generalized Einstein ratio D/(mu V_T) = u deta/du is the same statement made locally, and it holds because dln(gamma)/dln(u) = 1 - D/(mu V_T). docs/01-physics.md asks for the Einstein form; the effective potential form is chosen instead because it puts one term in one place, `_bernoulli_pair`, uses the same gamma the contacts and the equilibrium use, and leaves the Bernoulli argument the only thing that changes. Applying the ratio to D while leaving X = psi_right - psi_left would be a discretization error, not a modelling choice, because the exponential fit's decay rate is mu E / D and no longer 1/V_T |
| 2026-09-01 | The degenerate contacts and the degenerate Bernoulli argument have to land in one change, not two | The discrete equations enforce the Boltzmann relation exactly at equilibrium, so a contact carrying a Fermi-Dirac density and a Fermi-Dirac potential on a Boltzmann interior puts the whole 30.5 mV in a boundary layer one node wide. Measured on a 1e17 / 1e20 diode at equilibrium: ln(n) - psi is constant across all 201 nodes to 5.6e-17 and equal to exactly 0.0 at both contacts, and ln(p) + psi to 1.1e-16. That is the discretization holding n = n_i exp(psi) to the last bit, and it is what a half landed change would fight |
| 2026-09-09 | The density cap is applied to u itself in both directions, never by capping ln(u) and exponentiating | The forward path caps n/Nc at 8 by comparison and gets exactly 8.0. The inverse solves for w = ln(u), and capping w at ln(8) then exponentiating gives 7.999999999999998, one ulp below the ceiling. The slope helper returns zero at or above the ceiling and the series slope below it, so that one ulp made the Newton tangent 3.4 times too steep. Measured: at psi = 30 the inversion then converged linearly instead of in one step and the round trip ln(n) against the effective potential came back 0.306 out after six steps, against 0.0 exactly with the cap applied to u. The two directions have to cap by the same comparison or they are not inverses of each other |
| 2026-09-09 | Fermi-Dirac is a device level flag, off by default, and the NMOS is the only device that turns it on | It is a modelling choice rather than a number silicon has: the same silicon is degenerate or not depending on how heavily this device is doped. Off by default means every result from Phases 1 to 4, including the five DEVSIM benchmarks, is bit for bit what it was, because every assembly takes None and does nothing with it rather than treating Boltzmann as a special case of the degenerate path. The NMOS turns it on because its source and drain are at 1e20, where n/Nc is 3.5, and phases/PHASE-5.md scope item 2 asks for it |
| 2026-09-09 | Degeneracy enters the coupled system as two Bernoulli arguments, not one | The electron flux is fitted in psi + ln(gamma_n) and the hole flux in psi - ln(gamma_p), so a degenerate device evaluates two Bernoulli pairs per edge where a Boltzmann one evaluates one. That makes the argument depend on the densities as well as on the potential, so the two diagonal continuity blocks pick up the same edge conductance the potential blocks already carry, weighted by d psi_eff/d n. On a flat 1e20 bar the resulting edge coefficient is D/h times the generalized Einstein ratio, 2.13, which is the physical content: degeneracy raises the diffusivity. Verified block by block against complex step |
| 2026-09-09 | A field dependent diffusivity keeps reading the real potential drop, not the effective one | Caughey-Thomas is a statement about the electric field, and the field is the gradient of psi. Following the effective potential would make the mobility change when a density changes, which the model does not say, and would put a term in the Jacobian that complex step immediately disagrees with. So the Bernoulli pairs move to psi_eff and the diffusivity does not |
| 2026-09-09 | A DeviceState carries the statistics its quasi-Fermi levels are read under | phi_n is psi_eff - ln(n), and a density with a potential does not by itself say where the Fermi level is. Reading a degenerate state with the Boltzmann formula is wrong by the full 30.5 mV, and the nonlinear Poisson block then holds that wrong level fixed while psi moves, so it is a different equation rather than a reporting slip. Measured before the state carried it: on a 1e17 / 1e20 diode the converged Gummel answer was not a fixed point of the coupled system, and the coupled Newton moved the current by 2.4e-4 relative at every bias. With it, Newton starts converged |
| 2026-09-09 | The Gummel continuity blocks lag the degeneracy correction at the incoming density | A Gummel block exists because continuity is linear in its own carrier once the other two unknowns are held, and the degenerate Bernoulli argument is not. Lagging it is the same treatment this path already gives a field dependent diffusivity. It costs a convergence rate Gummel never had and leaves the fixed point alone, which is checked rather than asserted: the coupled Newton handed a converged degenerate Gummel state at 0.3 V has nothing left to do |
| 2026-09-09 | The complex step Jacobian reference is not usable on a degenerate junction at equilibrium | tests/reference/complexstep.py already records that Im(B(x + ih)) loses about 2 eps / abs(x), so the harness is worse than the code it checks for edge drops between zero and roughly 1e-5. A 1e20 region at equilibrium is flat to 1e-15 but not to zero, which is squarely inside that band. Measured on a 1e17 / 1e20 junction the harness disagreed with the assembled dF_n/dn by 1.4e-4 while a real central difference agreed with it to ten digits. So the degenerate blocks are verified at a flat state, where every drop is exactly zero and the harness is rescued exactly, and at a perturbed state, where every drop is of order 0.1 |
| 2026-09-09 | test_the_off_state_current_is_left_alone was replaced by a depth profile, because it was reading a current below its own Kirchhoff residual | It compared the two surface mobility transfer curves at zero gate bias and asked them to agree to 2 percent. On that mesh the drain current there is 5e-8 while the sum of the terminal currents, which Kirchhoff says is zero, is 3e-8: the quantity is a cancellation between fluxes ten decades larger and carries no information. It agreed to 0.8 percent under Boltzmann and to 59 percent under Fermi-Dirac, and neither number was about mobility. The claim it wanted, that the correction is a surface term and not a constant knocked down everywhere, is now read off the mobility itself: 0.37 of bulk on the interface row, inside a percent of bulk a third of the way into the substrate, exactly 1 in the oxide |
| 2026-09-09 | The residual a coupled solve tests for convergence is measured row by row, and the preconditioner that divides the system stays one number per family | These were the same number until now and they cannot be. A residual is a difference of terms, so the terms of its own row are the only thing it can be resolved against, and on a 1e17 / 1e20 junction the electron flux terms span 11.5 decades between the two sides: measured against one number for the family, every row in the lightly doped side is divided by something set on the degenerate side and lands below any threshold whatever it says. Measured: a cold solve_bias_newton at 0.4 V returned converged after zero Newton steps, still sitting on the equilibrium guess, reporting 1.2208e-10 of current against the 7.3648e-4 the Gummel path gives on the same device. 6.8 decades, with no failure reported, and the residual it reported at that guess was 5.1e-12. Under the per row measure the same guess reads 9.7e-1 and the converged answer reads 7.1e-15, reached in three steps rather than zero. The preconditioner cannot follow it there: a row of dF_n/dn holds conductances of size D*face/h while its residual holds D*face/h*n, so dividing the Jacobian by the per row residual scale leaves entries of size 1/n and hands the factorization a worse conditioned matrix. Not a different step, because a diagonal left preconditioner does not change the exact solution of J dx = -F and the two steps measure identical; what changes is how much of it survives an LU in double precision. Condition numbers of the same assembly under the two weightings: on the 1e17 / 1e20 junction at 0.4 V, 1.5e15 per family against 5.9e16 per row; on the MOS capacitor at 2.6 V of accumulation, 6.1e19 against 4.2e24, five decades and past where double precision means anything. Unscaled is 8.5e19 and 3.0e19 respectively, so per family scaling buys four decades on the junction and per row scaling spends five on the capacitor. So row_weights keeps one number per family for scale_rows, residual_measure multiplies that back out and divides by the row's own terms, and the two are documented as not interchangeable |
| 2026-09-09 | newton_solve takes how to measure a residual as an argument rather than fixing it at max abs(F) | max abs(F) is right exactly when every row is measured against the same thing, which scale_rows stopped being true of the moment the scales went per row. The alternative was to recognise the coupled system inside the generic solver, which would put device physics in ddsim/solve. Whatever the callback returns is also what lands in residual_history, so the number reported and the criterion applied cannot drift apart. The default is unchanged, so every other caller of newton_solve is bit for bit what it was |
| 2026-09-09 | The gate length sweep holds one process fixed and varies only L_gate | Roll-off is a statement about one process drawn at different gate lengths, which is what a wafer holds. Scaling the oxide, the junction depth and the channel doping alongside the gate would also produce a Vth against Lg curve, and no part of it could be attributed to the channel getting shorter, because four things moved at once. So SHORT_CHANNEL_PROCESS is a process sized for the 50 nm end, a 2 nm oxide over a 1e18 channel with 25 nm junctions reaching 10 nm under each gate edge, and the 1 um device is that same process drawn long. The channel doping is what holds the 50 nm device out of punch-through and the encroachment is what leaves it 30 nm of metallurgical channel. Measured across 1 um to 50 nm: Vth falls 184 mV by constant current and 177 mV by linear extrapolation, DIBL rises from 8.8 to 126.8 mV/V, the subthreshold slope goes 72.7 to 87.8 mV/decade, and the saturation exponent falls from 1.761 to 1.030. Remeasured after the 1/x_0 row below, which moved every constant current threshold up by about 180 mV and left the extrapolated ones alone. That the two methods now agree to within a few millivolts at every gate length, 0.2782 against 0.2806 at 1 um where they used to differ by 180, is a check on the fix that nothing in the fix was fitted to |
| 2026-09-09 | The constant current threshold target moves as 1/L across the sweep, Id = 100 nA * W / L | A shorter device drives proportionally more current at the same overdrive, so a fixed current target reads that geometry as a threshold shift and hands back roll-off the extraction put there. Dividing by L removes the trivial part. It is also the standard statement of the method, so the numbers are comparable to a data sheet |
| 2026-09-09 | The subthreshold slope of a sweep point is read over the two decades of drain current below the constant current threshold, not over the whole curve | The bottom of a solved transfer curve is not channel current. It is the reverse leakage of the drain junction, which flows the other way and does not care what the gate is doing, on top of arithmetic noise of the same size, because the terminal current there is the difference of much larger edge fluxes. Subtracting a constant from an exponential leaves something steeper than the exponential, so the minimum slope over the whole curve lands on the step out of that floor. Measured on this sweep, the whole curve minimum reads 36.0 mV/decade at 200 nm and 38.3 at 70 nm, both below the 59.5 thermal limit, while 1 um and 100 nm read 68.5 and 71.7 and look fine. Over the windowed measure the same devices read 72.6, 72.6, 73.4, 76.3 and 87.1 on constant mobility and, after the 1/x_0 row below moved the threshold the window hangs from, 72.7, 73.6, 74.5, 78.4 and 87.8 on the full model stack, monotone and every one above the limit. Two decades because it is wider than any reasonable gate step and still clear of the knee |
| 2026-09-09 | The saturation exponent takes its overdrive from the linear extrapolation threshold, not from a constant current one | The square law is a strong inversion statement, and the tangent at peak transconductance is the threshold that belongs to it. A constant current threshold sits several decades lower, in the knee where the curve is still leaving its subthreshold exponential. Measured on the 1 um device on constant mobility, where it is nearly a clean square law: overdrive from the constant current threshold fits 3.41 to 4.20 depending on the window, while overdrive from the extrapolated threshold gives 1.88 to 1.95 and barely moves with the window |
| 2026-09-09 | nmos falls back to a uniform channel mesh when the gate is short enough that uniform is already finer than h_min_x | h_min_x is what the junction wants resolved, not a floor on cell size. Grading trades a coarse far end for a fine near one and has something to trade only while even spacing would leave cells coarser than h_min_x. At a 50 nm gate the half channel spreads its 16 columns at 1.7 nm with h_min_x asking for 2, so graded_mesh_1d refused the request as infeasible and the short end of the sweep could not be built at all |
| 2026-09-09 | The gate length sweep builds its own transport models with Arora, Lombardi and Caughey-Thomas on, rather than taking TransportModels.for_device's defaults | for_device defaults to the Phase 2 constant mobility with no field dependence and no surface term, which is correct for it, since every result recorded before Phase 5 was taken that way. gate_sweep inherits those defaults, so the first sweep I ran was on constant mobility and its saturation exponent was measuring something that contained no velocity saturation at all. Scope items 3 and 4 of phases/PHASE-5.md name Lombardi and Caughey-Thomas as not optional. Measured, the difference is not cosmetic: peak transconductance at 1 um falls from 2.567e2 to 2.119e1 A/(cm V), a factor of 12, and the exponent at 50 nm from 1.359 to 1.033. Both transconductances were measured before the 1/x_0 row below and are 244.59 times what they should be; the factor of 12 between them is what this row is about and a constant on both leaves it alone |
| 2026-09-09 | Velocity saturation is attributed by switching Caughey-Thomas on one device, not by reading the exponent | Several things move together as the gate shortens, so a falling exponent on its own is consistent with two dimensional field effects, source and drain series resistance, or the threshold reference moving. Solved twice at Vg = 1.2 V and Vd = 1.0 V with only the lateral field model switched and Lombardi on in both, which is the stack the phase reports on: at 1 um the drain current goes 2.0673e3 to 2.0483e3 A/cm, 0.9 percent, and at 50 nm 5.6074e4 to 3.3540e4, 40.2 percent. Those four currents were measured before the 1/x_0 row below and are 244.59 times what they should be; the two percentages are ratios taken within one device and a constant on both arms leaves them alone, which is why this row stands. With Lombardi off in both the same two numbers were 2.9 and 44.8 percent, so the surface model neither carries the attribution nor hides it. It was off in both until 2026-09-09 because the combination could not be tested at all, see the divide by zero row below. The exponent decomposition at 50 nm is 1.233 with neither mobility model, 1.160 with Lombardi, 1.033 with both, against 1.972 for the 1 um device with neither, so most of the fall from 2 is geometry and the two mobility models take the rest. The README says that rather than crediting all of it to velocity saturation |
| 2026-09-09 | Matthiessen's rule in LombardiSurface names the infinity rather than dividing to reach it | mu_sr is delta E_perp^-gamma and gamma carries the carrier density linearly, so a state holding far more carriers than doping underflows it to exactly zero. The limit is right and wanted: no surface roughness channel means no mobility. Reaching it as 1/mu_sr costs a divide by zero warning, and `filterwarnings = error` turns that into a failure, so the combination surface=True with field_dependent=False could not be tested at all. The state is not a bug either: initial_state puts n = n_i exp((psi - phi_n)/V_T) at the drain, which past about 0.3 V of drain bias is 1e36 cm^-3 before Newton has taken a step, and a model handed an iterate has no say in which iterate it is handed. np.divide with where=mu_sr > 0 and out=inf gives the same 0.0 with no flag. Bit for bit identical wherever mu_sr is positive, checked over 200000 samples spanning 23 decades, so no earlier result moves. mu_ac is a sum of positive terms over a floored field and the bulk mobility comes from Arora, so neither can vanish and neither is guarded |
| 2026-09-09 | _surface_moved reports an infinite move where the diffusivity it is measuring against was zero | The fixed point asks how far the refreshed mobility moved relative to the one it replaced, which has nothing to divide by once an edge carries no mobility. The same cold guess as the row above produces three such edges on the coarse MOSFET at 0.3 V of drain bias and none at 0.05 V, which is why a phase of tests ran this combination without meeting it. An edge that went from nothing to something moved by everything, so inf is the honest answer and it correctly refuses to call the fixed point converged; an edge both sweeps agree has no mobility reads zero rather than nan. It costs at most one extra sweep and only on a cold start. Bit for bit identical wherever the baseline is positive, checked the same way |
| 2026-09-09 | A 2D terminal current was too large by 1/x_0, and every 2D current this project has ever reported was wrong by that factor | A continuity residual is a divergence integrated over a dual cell, and a dual cell scales as x_0^d, so converting one back to a current carries J_0 * x_0^(d-1). `terminal_currents` multiplied by J_0 alone, which is right in 1D and short one factor of x_0 in 2D. On a device scaled at 1e17 that is 244.59. Found while building the DEVSIM MOSFET benchmark, before the benchmark existed: the two codes disagreed by a factor that was flat at 244 across six decades of drain current and through both the exponential and the linear regime, with the step to step ratios agreeing to under a percent, which is a scale error rather than any physics. Confirmed as exactly 1/x_0 to thirteen figures and invariant under mesh refinement in either direction, device height, doping and bias. Nothing caught it and nothing could: a constant factor on every terminal leaves Kirchhoff satisfied, leaves threshold voltage, subthreshold slope, DIBL and the saturation exponent alone because all four are shapes, and never reaches the 1D diodes because x_0^0 is 1. The MOS capacitor is 2D and agrees with DEVSIM to 0.053 percent, but it compares a charge. Until tests/analytic/test_ohmic_resistor.py grew a 2D sibling there was no case anywhere that compared a 2D current against a number worked out without the solver. After the fix ddsim and DEVSIM agree on the 1 um NMOS to 0.21 percent at Vg = 2 V, 0.37 at 1.5 V, 0.66 at 1.2 V and 1.23 at 1.0 V on two independently generated meshes, against the 5 percent benchmark 6 asks for |
| 2026-09-10 | The gate ramps inside tests/analytic/test_mosfet_rolloff.py and tests/unit/test_coupled_transport.py are `continue_to` rather than a fixed list of biases | Both were hand written ramps with no answer to a step that misses the basin, and both passed here and failed on Linux in CI. The basin of the undamped coupled solve is not an interval and stepping into it is not monotone in bias. Measured on the coarse MOS capacitor of the test, 41 silicon nodes and 3 oxide, one solve_bias_newton per point from the same equilibrium guess at 0 V: -0.05 converges in 22 steps, -0.10 in 13, -0.15 in 9, then -0.20, -0.25, -0.30 and -0.40 all fail, then -0.50, -0.75 and -1.00 converge again in 18, 19 and 16, then -1.25 fails. The three that converge past half a volt do it on the limiter, 3, 8 and 9 of their steps against it, which is what a trajectory that is wandering rather than descending looks like. -0.02 is its own case: it ends 30 steps at a residual of 8.161e-12, below the 1.010e-10 threshold, and is refused on the update criterion alone, because the true step there is smaller than the roundoff of an ill conditioned solve and the iterate cycles at 1e-5 forever. The Jacobian is not what is wrong: at -0.10 the tail is 1.0e-5, 1.5e-9, 6.1e-13, 2.2e-15 with updates falling to 1.7e-11, which is quadratic to machine precision. CI read the other side of the same coin, a residual of 7.095e+10 on the capacitor and 9.889e+03 on the 2835 node MOSFET, and on Python 3.11 and 3.12 an overflow in the Caughey-Thomas square from an iterate that had wandered, which `filterwarnings = error` turns into a failure. Continuation halves the step and retries, and the -1 V point it now lands is reached in 3 attempts. No number moves: Newton is converged to 1e-10 at the same bias either way, so the root is the same and only the path to it changed. Both tests were checked against the mutation they exist to catch afterwards, the gate target built from 0.0 instead of the applied bias for the capacitor, which converges and hands back psi identical to the unbiased solve to the last bit, and Caughey-Thomas made a no op for the rolloff, which leaves the two arms at the same 229.258 A/cm |
| 2026-09-10 | The exact equality test on the Boltzmann contact compares against math.asinh and not np.arcsinh | The test asserts that ohmic_psi_scaled with no statistics is the same arithmetic rather than an approximation of it, which is worth asserting exactly. It was reading np.arcsinh, which is a second implementation of the same function, so what it actually compared was two C libraries against each other. They differ in the last two bits on some platforms: CI read 23.025850929940454 from the Boltzmann branch and 23.025850929940457 from numpy on Python 3.11 and 3.12, and the same assertion passed on 3.13. math.asinh is what the branch calls, so comparing to it keeps the claim exact and drops the dependence on which libm the runner shipped |
| 2026-09-10 | A cold coupled solve at a large applied bias ramps the bias in as a fraction of itself, through solve_bias_ramped | The Poisson guess knows nothing about the applied bias, so the first Newton step wants a potential update far larger than max_psi_step allows and is clipped. Measured on the 2835 node 1 um NMOS of SHORT_CHANNEL_PROCESS, cold from that guess: 0 V is one step with nothing clipped, 0.25 V is ten steps with nothing clipped, 0.5 V is twelve with two clipped, and 1 V is twenty two with twelve clipped. Half a budget spent against the limiter is not a basin, it is arriving anyway, and which side of it a machine lands on is set by the order the linear solver sums its floating point. CI landed on the other side on all three Python versions and reported a residual of 9.889e+03 with psi over [-22.2, 62.9], on the solve that converges here. The ramp is the same continuation the gate sweep already runs between its points, applied to the point it starts from, and the final step is taken at the device's own bias unconditionally so that a stalled ramp can never hand back a converged solve at a fraction nobody asked for. It is used by gate_sweep for its first, cold point and by the velocity saturation attribution test, and nowhere warm: a continuation step already arrives with a better guess than this one. Not damping and not a change to the Jacobian, which is quadratic at every fraction. The roll-off table in README.md was regenerated through it and every digit is unchanged. |
| 2026-09-11 | The DEVSIM MOSFET benchmarks, 6 to 8, run both codes at Boltzmann statistics and constant mobility rather than at the Phase 5 stack | docs/04-validation.md is blunt that an unmatched model makes a comparison meaningless, and the reference has to carry whatever ddsim carries. Arora is a closed form and would go into devsim as one node model, but Caughey-Thomas and Lombardi both need element quantities in devsim, the parallel field along the element and the field normal to the interface, and Lombardi in particular is only reachable through devsim's own Klaassen.py and mos_physics.py, which replace the edge based current with an element based one. That is a second discretisation, not a second parameter set, and putting it in before a single transfer curve exists would mean debugging two unfinished things at once. So the benchmark set lands at the model set the diodes and the capacitors already share, which is enough to compare the 2D transport, the geometry and the electrostatics. Threshold voltage, subthreshold slope and DIBL are made of those. Drain current magnitude and the saturation exponent are not, and the currents these benchmarks compare are therefore not the currents the README roll-off table reports, which are taken with Arora, Caughey-Thomas and Lombardi on. ddsim is run with degenerate=False for the comparison so that both sides are Boltzmann, the same choice diode 3 already documents |
| 2026-09-11 | The devsim MOSFET generator hands `solve` a relative tolerance it can actually reach, backing off 1e-8, 1e-6, 1e-4 until one returns, rather than the 1e-10 devsim's own examples use | devsim converges a solve when the relative update is under `relative_error` and the absolute update is under `absolute_error`. Only the relative one binds, because the absolute one is a norm mixing volts with cm^-3 and no single value of it means the same thing for both, which is why it is set to 1e30. The relative one has a roundoff floor and 1e-10 is below it. Measured on the 3096 node 1 um device at 0.3 V of gate, from the exact equilibrium start: the electron continuity relative update sticks at 2.17340e-09, the identical figure at iterations 2, 3, 4, 5 and 6, while the absolute update wanders between 1.03e+04 and 4.69e+05 cm^-3 on a density of 1e20. That is a converged solve cycling in its own roundoff and being refused as a convergence failure. It is the same reading docs/07-decisions.md already records for ddsim's own MOS capacitor at -0.02 V, where an iterate below the residual threshold was refused on the update criterion alone. The tolerance is therefore a step size control and not an accuracy control: `settle` owns accuracy by watching the solution stop moving, and a devsim solve that fails is rolled back by devsim, so trying a looser one after it costs a call and damages nothing. The ladder stops at 1e-4 because a looser tolerance than that returns from the first Newton iteration before devsim's own damping has engaged. What it buys, measured on the 1 um device at 50 mV of drain: the gate walk used to stop dead at 0.26 V, unable to cross the inversion knee at a step of 0.1 mV, and now runs 0 to 1.5 V in sixteen points in under two minutes, agreeing with ddsim from 0.23 percent at 0.3 V to 4.3 percent at 0 V |
| 2026-09-11 | The devsim generator assembles on one thread | devsim splits assembly across a thread pool by default, so the order of a floating point sum is set by how busy the machine is. The same mesh at the same bias converged in one process and raised a convergence failure in another under nothing but a different load, which makes the difference between a golden file existing and not. Golden data that depends on machine load is not golden |
| 2026-09-11 | The devsim MOSFET mesh coarsens every spacing except the one at the silicon surface | The 1 um mesh went from 11168 silicon nodes to 3096 and a solve call from 7.6 s to 0.4 s, which is what makes a sixteen point curve affordable. Measured one spacing at a time on the 1 um device at zero gate and 50 mV of drain, which is drain junction leakage and so the most mesh sensitive point on the curve: relaxing the surface row spacing alone from 5e-8 to 2e-7 moves the drain current from 1.5612e-6 to 2.1849e-6 A/cm, 46 percent, while relaxing the junction columns from 2e-7 to 5e-7, the channel columns from 5e-7 to 2e-6 and the implant depth rows from 5e-7 to 1e-6 together move it by 5 percent and move every point above threshold by well under one. The inversion layer is a nanometre thick and is the only structure on this device a mesh can miss. The two short benchmarks set their own column spacings, since half a micron is wider than their whole gate |
| 2026-09-11 | The devsim generator ramps the drain onto the coupled system instead of applying it before the first coupled solve, and its bias ramp only grows the step after four steps in a row have converged | Two separate ways of landing outside a basin. Handing the freshly built drift diffusion system a device that already has 50 mV across its drain junction is the same unramped jump ddsim's own cold solve was fixed for the day before: the same mesh converged from it in one process and failed in another. Growing the step by half again after every success is what devsim's own python_packages/ramp.py pointedly does not do, and the reason shows at the inversion knee, where the step that converges is a millivolt and the one above it does not, so the ramp spends every other step on a failure and the halving that follows eventually takes the step under min_step and aborts a walk a constant millivolt would have finished |
| 2026-09-11 | The devsim generator does not pin MKL to one thread, even though that is what would make a run bit reproducible | The row above pins devsim's own assembly pool, which is free. The factorisation is the other half and it is not: devsim's `direct_solver` is `mkl_pardiso`, and `MKL_NUM_THREADS=1` set before the devsim import does remove the last of the run to run variation. It also turns `build_physics` on the 1 um device from about four seconds into more than three minutes, because the arithmetic it lands on walks a different and much worse path through the drain ramp, and it did the same to two full transfer curves that had each taken two minutes. A reproducible generator that does not finish is worth less than one that does. What absorbs a different path instead is the tolerance ladder in `solve_once`, which retries a refused solve rather than reading the refusal as divergence. What is left, measured across runs on the 1 um device, is about 3e-5 of relative drain current at zero gate and less above threshold, four decades under the tolerance any benchmark asks for |
| 2026-09-11 | The devsim MOSFET generator deletes each device as its curve finishes | devsim's `solve` takes no device argument. It solves every device in the simulation at once, so a generator that builds one device per curve and leaves it lying around is asking devsim, on the second curve, to converge two devices simultaneously, one of them already converged and contributing nothing to the shared error norm but its own roundoff. This is what was behind a whole class of symptoms that looked like bad luck: the identical equilibrium Poisson problem, same mesh and same biases, converging on the first device of a process and raising a convergence failure on the second. Measured directly: building the same 1 um device four times in one process fails on the third, and deleting each one as its curve finishes makes all four succeed |
| 2026-09-11 | The devsim equilibrium Poisson solve starts from charge neutrality rather than from the zeros devsim hands it | devsim creates a solution array full of zeros and its own examples solve from there. On this device that starts 0.60 V away in the source and drain and 0.48 V away in the body, and the walk back is long enough to be luck. The charge neutral potential is exact everywhere except inside a depletion region, so seeding it leaves a small well conditioned correction instead. It is the same guess ddsim's own ohmic contact makes, written as `V_t ln((N + sqrt(N^2 + 4 n_i^2)) / 2 n_i)` because devsim's expression parser has no asinh. An initial guess moves no converged answer, it only decides whether one is reached: the 1 um equilibrium drain current is 1.5612e-6 A/cm with it and without it |
| 2026-09-11 | The devsim MOSFET generator leaves the two continuity equations on devsim's `positive` variable update at every drain bias, rather than switching them to `log_damp` at high drain | `positive` scales the whole Newton update so no density anywhere goes negative, and in a pinched off channel the update near the drain wants to move by decades, so one node sets a factor that throttles every other node. That reads like the cause of the 1 V wall, where the outer iteration stopped converging and the drain current wandered from 1.243e-1 to 9.23e-2 A/cm over fifty passes. `log_damp` bounds each density per node instead and cannot be throttled by one node, and it is what devsim already uses for the potential and the oxide, so it looked like the fix. It is not. Measured as a controlled pair on the 1 um device, one high drain curve each, same process shape, run one after the other so neither saw the other's load: with `log_damp` on above 0.5 V of drain the curve stalls going from Vg = 0 to Vg = 0.1, after 574 s, with no step above 0.1 mV converging, which is worse than the wall it was meant to remove. With it off the same curve walks the whole sweep, crossing the Vg = 0.3 knee in about fifteen minutes and then accelerating. An earlier cross run comparison appeared to show it helping, and that comparison was worthless because the two runs also differed by the stall detection commit. What made the high drain curve reachable is therefore something other than the update rule. The only changes between the run that died at Vg = 0.4 and the one that walks the whole sweep are the settle stall detection and the streak based ramp growth, so those are the candidates, but that attribution is inferred from the commit order and has not been measured as a pair the way this row's own claim was |
| 2026-09-11 | The devsim generator's halved mesh self check compares both transfer curves and reports the worse, rather than the low drain curve alone | `sweep(refine=2.0)` solves both curves on the halved mesh and the check read only the low one, so the high drain curve, which is the expensive and fragile half, was solved and discarded. The number it writes into the header is read by `test_devsim_mosfet.py::test_golden_reference_is_converged` as the reference's mesh error for both curves, so reporting only one of them overstated what had been checked. The refined run already pays for both, so using both costs nothing |
| 2026-09-12 | The devsim generator's `settle` watches terminal balance as well as the potential, on the one state per bias point that gets written down | The potential is the Poisson residual and it arrives one to two decades before the continuity one, so a settle that watches only the potential returns while the current is still moving. Measured on the 1 um device at zero gate and 50 mV of drain: on the pass `settle` returned, the largest potential move was already inside 1e-9 V and drain and source disagreed by 12.7 percent, and twenty six further passes took that to 1.8e-5 while the drain current moved 0.70 percent, from 1.453019e-6 to 1.442909e-6 A/cm. That is the difference between a golden point that fails `test_golden_terminals_balance` and one that passes it by three decades. The balance is a target and not a requirement: past its floor the imbalance bounces while the drain current holds to seven figures, so a stalled balance is accepted rather than failed, and exhausting the pass budget on the balance alone is accepted too, because the potential has converged and halving the bias step cannot help a quantity the step size does not control. It does not reuse the potential's stall test, and trying to cost a full run: the potential either converges or bounces, so any improvement is progress, while the imbalance has a third behaviour and crawls. The same zero gate solve that reaches 1.8e-5 in 26 passes on one run inched to 5.1e-3 over 400 on another, gaining a little on most passes, resetting the counter every time and then raising at the budget. Which of the two a run gets is set by the order MKL sums a factorisation, which the row above deliberately leaves unpinned. A new best now has to beat the record by a tenth, which keeps every genuine descent, since the early passes gain 12 to 22 percent each, and ends a crawl in `stall` passes. Only the recorded state pays for it. Every intermediate step of `ramp_to` keeps the old criterion, because those states are never reported and buying twenty extra passes for each of them buys nothing: measured across the curve, Vg = 0 takes 26 extra passes and 8 s and every other bias takes one pass and no measurable time |
| 2026-09-12 | The devsim generator's row spacing through the implant is derived from the implant sigma, 0.25 of it, rather than the flat 1e-6 cm it was | 1e-6 cm is 1.21 sigma on this process, more than a standard deviation per row through a Gaussian, which puts the metallurgical junction in the wrong place. It is the whole of the reference's convergence problem. The generator's halved mesh check on the 1 um device reported 4.6 percent at zero gate and 2.9 at 0.1 V against a gate of 0.5, and splitting the halving by axis, which the single `refine` knob had never allowed, put 0.036 percent of it on the lateral columns and 2.898 of the 2.933 on these rows. At 0.25 sigma the same check reads 0.53 percent at zero gate and under 0.08 everywhere else, and going on to 0.125 sigma moves the answer a further 0.015 percent, so the profile is resolved rather than merely finer. It costs 8514 silicon nodes against 3483. ddsim needed no matching change and did not get one: it grades its rows from `n_silicon` and `h_min_y` rather than naming a spacing, and already samples this implant at 0.21 sigma at the junction depth and 0.40 at worst within two junction depths of the surface. `test_ddsim_resolves_the_implant` holds it under 0.5 sigma, and that line is placed rather than guessed: it passes on the two finest rungs of ddsim's own vertical ladder and trips on the two coarsest, which are exactly the rungs whose drain current was still moving 1.47 and 4.79 percent. So the roll-off table in README.md does not move. What moves is the reference: the 1 um zero gate point goes from 1.453019e-6 to about 1.394e-6 A/cm, 4.1 percent, and the earlier 0.23 percent agreement at 0.3 V was measuring two meshes that were wrong together |
| 2026-09-12 | The generator's mesh convergence metric skips a point when the two meshes differ by less than the points know about themselves, and records how many it skipped | A convergence check assumes the refined answer is the better one, and in the off state of a MOSFET that does not hold. The drain current there is a small difference of much larger fluxes and a finer mesh has more edges to lose digits across, so the imbalance floor gets worse as the mesh gets finer. Measured on the 1 um device at zero gate and 50 mV: the shipping mesh balances to 1.65e-4, and the halved mesh it is checked against settles to a largest potential move of 1.1e-16 V, holds its drain current to ten figures across 120 further passes, and still leaves drain and source 0.99 percent apart. That is not an unconverged solve, it is a converged discrete solution whose terminal currents do not cancel. The two meshes differ by 0.532 percent at that point, a quarter of what the finer of them knows about itself, so calling that gap discretisation error is reading noise as signal, and no refinement reachable on this mesh generator would reduce it. The rule is the one the comparison tests already use, three times the point's own imbalance, applied to the one place that was missing it, and it is measured rather than chosen. What it is not allowed to become is a way of reporting a small number by measuring almost nothing, so the skipped count goes in the header and `test_golden_reference_is_converged` fails if more than half the curve was skipped |
| 2026-09-12 | `build_mesh` refuses a mesh whose boundary row did not land, rather than letting devsim report it three calls later as a missing contact | devsim discards a requested `add_2d_mesh_line` when the rows graded up to it already put a node close enough, and keeps the node's coordinate. Asking for a surface spacing of 1.5625e-9 cm under rows graded from the implant depth lands the top silicon row at 9.99998817e-5 against a `t_si` of 1e-4, a gap of 1.18e-10 cm, and the silicon then has no node on the interface at all, so both surface contacts and the si_ox interface are created empty. devsim says nothing about the interface and mentions only the first contact anything tries to use. A mesh that lost its source contact is not a coarser device, it is a different one, and a convergence ladder that walks into one is comparing two unrelated transistors. The check is three questions at build time, all cheap: the top silicon row equals `t_si`, `get_contact_list` has all four contacts, `get_interface_list` has si_ox. It is what caps the surface refinement, and knowing that is the point of it |
| 2026-09-12 | `generate_mosfet.py` puts the repository root on `sys.path` | It imports `parameters` by package path, because the test suite imports it that way too, and run as the script its own README documents nothing puts the root on the path, so it died with `No module named 'tests'`. Its sibling `generate_diodes.py` sidesteps this by importing `parameters` flat, which this file cannot do. Five lines, and the documented invocation works |
| 2026-09-12 | `residual_measure` skips a row whose term scale has fallen below eps times the largest in its family, not only one that is exactly zero | The measure divides each row's residual by its own terms so that max |F| means the same thing everywhere, and the rows it was introduced for are the ones a per family scale used to certify falsely. A row can also go the other way. The columns of the assembly span the whole family, so the linear solve delivers each unknown to an absolute accuracy set by the largest terms rather than to a relative one against its own, and a row whose terms have collapsed is being asked for a relative residual double precision cannot deliver. Dividing its already converged residual by terms that small manufactures a number out of roundoff, and because it is roundoff it wanders instead of settling, so it never trips the frozen residual guard in `newton_solve`, whose test is exact equality over a window, and the convergence test becomes a coin flip per iteration. Measured on the 1 um NMOS of `SHORT_CHANNEL_PROCESS` at Vd = 1 V, Vg = 1.5 V: every raw residual in the system was at machine epsilon, the worst 5.3e-15, and the reported measure was 3.8e-10 against a threshold of 1e-10. All of it came from the electron row of node 2810 in the substrate, raw residual 9.0e-20 over terms of 2.4e-10, 23 decades under the 6.4e+13 the family reaches in the source, and the denominator shrinks as the gate drives deeper because the bulk minority population empties: 4.16e-10 at Vg = 1.2 and 2.38e-10 at Vg = 1.5. So the iterate stopped moving at 7e-15 by step 5 and the solver then waited for noise to land under the threshold, taking 8, 14 or 22 iterations at the same bias depending only on the path taken to reach it, and on CI exceeding the budget of 30 at Vg = 1.4 on all three Python versions, which stalled the high drain transfer curve at 1.3918 V and failed benchmark 6. The floor is eps times the family maximum and nothing tuned, 1.4e-2 against that row's 2.4e-10, so it binds by eight decades there while leaving every row the measure exists to catch: a scale one decade above the floor still counts, and `test_a_row_just_above_the_floor_still_counts` holds that line. Not damping and not a change to the Jacobian. It moves an answer only where the old path had already spun past convergence to reach it. Both 1 um transfer curves, floor off and floor on in one process: every point of the 50 mV curve agrees to 7.8e-16 or better and 12 of 16 bit for bit, and at 1 V every point is bit identical except Vg = 1.3, which moves 1.4e-10 in the last two digits because the old path spun 20 iterations past convergence to get there. What changes is the iteration count: 20, 13 and 22 at the last three biases become 5, 5 and 5, and the final residual is 1.2e-14 at every bias instead of scraping under 1e-10 by luck. The gate length sweep is the other path to check, because it runs the full Phase 5 stack through `_surface_fixed_point` rather than a plain Newton, and the figure it writes did change: `mosfet_rolloff.png` regenerated on the floor differs from the committed one in 8 pixels of 1091284, at a maximum channel delta of 1 in 255, same matplotlib and same dpi. That is anti-aliasing under a line whose data moved by less than a pixel, and a pixel on the threshold axis is 0.7 mV against the 0.1 mV the README prints, so the roll-off table does not move. The image is byte reproducible across two runs of the same code, which is what licenses reading 8 pixels as the whole of the difference |
| 2026-09-12 | The 180 nm reference's lateral column spacings are halved, to 2.25e-7 and 4.5e-7, and `build_mesh` takes a `refine_y` so the two axes can be halved separately | The device's first golden file reported 7.277e-3 against a gate of `0.1 * tolerance`, 5e-3, worst at +0.1 V of gate. On the 1 um device the same overshoot turned out to be almost entirely the rows through the implant, 2.898 of 2.933 percent, with the columns carrying 0.036, so the obvious move was to refine the rows again. That would have been the wrong axis. Halving one axis at a time, all three meshes solved in one process because the 2026-09-11 row records a cross-run comparison that was worthless for exactly that reason: columns alone move the drain current 6.9238e-3, worst at +0.1 V of the high drain curve, and rows alone move it 5.7227e-4, worst at +1.5 V. So this device sits on the opposite axis from the long one, which is what a short channel should do, since the lateral direction is where its structure is while a 1 um channel is laterally featureless and its error lives in the implant profile. The single `refine` knob could not ask that question, which the 2026-09-12 implant row already noted, so `build_mesh`, `transfer_curve` and `sweep` now take `refine_y`, defaulting to None meaning both axes together, and nothing about the existing check changes. The spacings were relatively coarser than the long device's rather than finer: at 9e-7 the gate spanned 20 columns where the 1 um spans 50, and at 4.5e-7 it spans 40. Note the axis experiment used `refine=2.0`, which halves `h_contact` too and gave 8382 silicon nodes, while this change leaves `h_contact` alone and ships 6402, so the mesh measured and the mesh shipped are not the same one: the experiment chose the knob and the regenerated file's own both-axes check is the number the gate is met by, 2.207e-3 with 0 of 16 points skipped, worst at +1.1 V. The worst point moving out of subthreshold into the on state is the sign the refinement landed where the error was. All ten of the benchmark's tests pass, `test_ddsim_matches_devsim_drain_current` on both curves included, so benchmark 7 of docs/04-validation.md is closed. The 65 nm device has the same 20 column gate and has not been measured yet |
| 2026-09-12 | The 65 nm reference's lateral column spacings are halved, to 8.125e-8 and 1.625e-7, and its convergence is accepted at 7.720e-3 against a gate of 8.0e-3 | Same axis as the 180 nm device and more of it. Halving one axis at a time from the original 20 column mesh: columns alone move the drain current 1.6606e-2 of a 1.964e-2 total and rows alone 2.6789e-3, both worst at -0.2 V, the deepest off state, with 0 points skipped. So the columns are again the error and the rows again are not, which is now two devices agreeing that a short channel puts its mesh error in the lateral direction while the 1 um puts it in the implant profile. What is different here is that the rows cannot help: 2.6789e-3 is already a third of this device's gate on its own, and it cannot be reduced, because most of it is `devsim_h_surface` at 6.25e-9 where the next halving is a third of an angstrom. That is also a correction to the `H_DEPTH_SIGMAS` docstring's claim that a quarter of a sigma is where the implant stops mattering: on the 1 um a further halving moves the answer 0.015 percent and on this device the whole vertical axis moves it 0.27, eighteen times more, so that conclusion was a 1 um conclusion. The columns therefore have to carry it alone. A quartering was tried first and abandoned: its shipping mesh solved both curves at 11022 silicon nodes and the halved mesh check behind it, about four times that, was killed for memory with 2.57 GB free of 16 GB. The halving ships 6930 nodes and its check completes. **The margin is 1.04x and that is on the record rather than glossed**: 7.720e-3 is 96.5 percent of the line, the extrapolation that chose the halving predicted 6.6e-3 from a lateral order of 2.07 measured off the 180 nm pair and was optimistic by 17 percent, and a different MKL summation order could plausibly put this device over. Raising the benchmark's 0.08 tolerance to widen the gate was rejected, since that loosens what ddsim is held to in order to flatter the reference. The quartered mesh would read about 2.7e-3, the vertical floor, and wants more memory than this machine had free. All 41 tests in test_devsim_mosfet.py pass, so benchmarks 7 and 8 of docs/04-validation.md are closed and tier 4 covers 1 to 8 |
| 2026-09-12 | Benchmark 9's "within 10 percent" is spent on the roll-off magnitude and on DIBL, not on Vth point by point, and the comparison runs at the matched Boltzmann and constant mobility set | Two decisions, both forced. First, the quantity. docs/04-validation.md asks for the trend and 10 percent and PHASE-5.md for the Vth against Lg curve to be within 10 percent, and Vth crosses zero in this set: devsim reads -0.1181 V at 50 nm and Vd = 1 V. A per point relative comparison on a signed quantity is not a measurement, since one absolute disagreement reads as two percent at one gate length and unbounded at another. So the percentage goes on the two positive quantities the phase actually names, the roll-off from 1 um to 50 nm and DIBL at each length, monotonicity in both codes at both drain biases carries the trend half, and per point agreement is reported in millivolts rather than gated as a fraction. Second, the model set. The roll-off figure runs Fermi-Dirac with Arora inside Lombardi inside Caughey-Thomas and the generator runs Boltzmann with constant mobility, so the drain current in these golden files is not the drain current that figure reports and the thresholds are not the README's: at 50 nm devsim gives +0.0065 and -0.1181 V where the README gives +0.0947 and -0.0258, because a constant current criterion rides on the current scale and mobility sets that. Both sides of this benchmark therefore run the reduced set, through `ddsim_curve`, which already builds with degenerate=False and constant mobility for benchmarks 6 to 8. `gate_length_sweep` is deliberately not used: its defaults are arora, field_dependent and surface, and it exposes no degeneracy argument, so it would have compared two different model sets and called the difference a disagreement. What licenses the reduced set is that the quantities being compared are electrostatic. Measured before the test existed, devsim's DIBL at 50 nm is 131.2 mV/V against the 126.8 the README reports from the full stack, 3.5 percent apart across two different mobility models, so the barrier the drain lowers is nearly mobility independent while the current through it is not. What this does not validate is Lombardi or Caughey-Thomas against an outside code, because nothing in tier 4 runs them. Phase 5 does not ask for that, its velocity saturation criterion being an internal emergent check, but the gap is real and is recorded here rather than left to be discovered |
| 2026-09-13 | Benchmark 9's threshold extraction drops the leading points whose drain current is not positive, and the drop is bounded at 1e-4 of the constant current target | ddsim's drain terminal current has a floor near 2e-9 A/cm, and at 50 mV of drain that floor lands negative: three points on the 200 nm device and two on the 100 nm, which made `threshold_constant_current` refuse the whole sweep and failed six of benchmark 9's tests before any physics was compared. It is a floor and not a current, and the curve says so: from -0.4 to -0.35 V the value moves in its fourth digit, -2.218683e-9 to -2.218135e-9, where a real subthreshold current changes by a factor of four across a 50 mV step, and the same flat value appears at +2.37e-9 on the high drain curve where the sign happens to land the other way. DEVSIM's golden curves need no trim at all, every point positive and strictly rising down to 2.4e-9 on the same device and the same bias, so this is ddsim's floor rather than a property of the sweep, and it is recorded as a deviation below. Nothing the extraction reads is lost: the 200 nm target is 5e-3 A/cm and every dropped point is nine decades under it, with 9 of 21 retained points still below the target to interpolate from. The trim cannot hide a solver problem, and that was demonstrated rather than argued. A leading point placed at 1 percent of the target, which is a real current and not roundoff, trips the 1e-4 bound and the assertion fires. A curve bent non monotonically at index 15, well above the floor, still fails `_rising` loudly, because the monotonicity check runs over everything kept. The trim is applied to both codes even though only ddsim needs it, for the same reason both go through one extractor: preprocessing one side and not the other would measure the preprocessing |
| 2026-09-13 | Benchmark 9's mesh convergence is gated at the tolerance itself, 0.1, not at a tenth of it, and the 50 nm reference ships at 1.227e-2 rather than being refined | The rule `test_golden_reference_is_converged` states is that the reference's mesh error must be small against what the benchmark asserts, and benchmarks 6 to 8 assert a drain current at every bias, so for them a drain current mesh error reaches the conclusion undiminished and a tenth of the budget is the right line. Benchmark 9 asserts a roll-off magnitude and a DIBL, and the attenuation between the two was measured rather than assumed: halving every spacing on the 50 nm moves its worst drain current by 1.227e-2 but its DIBL by 0.14 percent, 131.21 to 131.03 mV/V, and its saturated threshold by 0.343 mV, which is 0.11 percent of the 318.6 mV roll-off being compared. That factor near nine is not luck. The worst drain current point sits at -0.4 V, 400 mV below threshold and six decades under the constant current target, a bias the extraction never reads, while the extraction reads the curve near zero where the mesh moves it far less. So the gate is one factor of ten looser on the recorded current number and lands on the same tenth of the budget once the attenuation is counted, and 1.227e-2 sits eight times inside it. The axis split was run first, because the 180 nm and 65 nm rows both concluded that a short channel puts its mesh error in the lateral columns, and at 50 nm that conclusion fails: columns alone move the current 6.7214e-3 and rows alone 6.0427e-3, both worst at -0.4 V with 0 points skipped, adding to the 1.227e-2 measured together. So the two axes are equal here and neither can carry the reduction alone, which makes it two devices agreeing and a third disagreeing rather than a rule. Refining anyway would halve the columns to 80 across the gate, ship 14322 nodes and need a check near 57000, to improve a number benchmark 9 does not assert. Raising the 0.1 tolerance was never on the table, that being the thing ddsim is held to. The measured mesh shift, 0.343 mV, is larger than the code to code disagreement it sits inside, 0.1 mV at 50 nm, and both are far under the 10 percent gate; that ordering is worth knowing and is not a problem |
| 2026-09-15 | Benchmark 10 runs the same five devices as benchmark 9 a second time at the full Phase 5 stack, rather than moving benchmark 9 onto that stack | Benchmarks 6 to 9 match both codes at Boltzmann statistics and a constant mobility on purpose, so that a disagreement there is a statement about the 2D transport, the geometry and the electrostatics and nothing else. That is worth keeping. But run only that way, nothing in tier 4 put either mobility model or the statistics in front of an implementation that is not ddsim's, which the 2026-09-13 entry in PROGRESS.md named as the one real gap benchmark 9 left. Running both is what tells the two apart: a residual present in both is the device, and one present only in benchmark 10 is a model. It is also what the deliverable plot of phases/PHASE-5.md needs, since that figure draws the full stack and the benchmark 9 thresholds are not its thresholds: at 50 nm benchmark 9 gives +0.0065 and -0.1181 V where the figure reports about +0.09 and -0.03, because a constant current criterion rides on the current scale and mobility sets that |
| 2026-09-15 | devsim's Lombardi correction is frozen inside a solve and refreshed between solves, and the refresh happens inside `settle` rather than around a fully settled solve the way ddsim's `_surface_fixed_point` does it | The model needs the field normal to the interface, which no edge carries, so it is evaluated at nodes, and a nodal gradient is something devsim cannot differentiate. Freezing it is therefore not a choice, and it is the same trade ddsim makes for the same reason: the rate, not the answer, since at the fixed point the frozen correction is the one the answer implies. Where the two codes differ is where the refresh sits. ddsim can hold a coefficient still across a whole solve because its Newton carries damping and continuation. devsim's repeated `solve` diverges outright when handed a mobility frozen at the initial guess for a whole settle, so the refresh has to happen pass by pass |
| 2026-09-15 | `settle` throws away its stall record when the surface refresh phase ends | Every move it measured up to that point was taken across a refresh that moved the potential itself, so the record is not a record of the same quantity. Carried forward it is what failed these solves: `best` held a coincidentally tiny 1.8e-7 V from the refreshing phase, no later pass could beat it, and the stall fired at a last move of 1.7e-4 V while every devsim solve inside was reporting RelError of exactly zero. `ramp_to` reads a failed settle as a failed bias step and answers by halving, so a converged 1 um device at 1 V of drain died claiming no step above 0.1 mV converged |
| 2026-09-15 | `settle` accepts a potential that has stopped improving below `SETTLE_FLOOR`, 1e-5 V, as arrived instead of raising | A settle that is not going to arrive and a settle that has arrived and is bouncing in devsim's own roundoff look identical to a counter that only knows no pass beat the record. The two are two decades apart, measured on the 1 um at 1 V of drain and a gate of +0.55 V, which is the only bias on any of the five that needs this: the genuine failure above bounced at 1.730e-4 V and never came down, while the full stack with that fixed bounces and never improves again. The 1 um curve accepts three floors, at 2.794e-7, 3.752e-7 and 1.418e-6 V, so the value has seven times the headroom it needs on the run that produces the data, and a cold solve dropped straight onto the knee rather than walked up to it reaches 6.2e-6, the widest seen anywhere and still inside. The first value tried, 1e-6, was tighter than the bouncing and failed the run. Every accepted floor is printed to stderr, which is where it has to go: the recorded bias point settles inside `quiet`, so a diagnostic on stdout is written only on the passes nobody is watching and not on the one state that gets written down. The other four gate lengths reach the 1e-9 tolerance outright and so does the whole reduced set, which is why benchmarks 6 to 9 never needed this. Why it bounces at all is already recorded in `SOLVE_TOLERANCES`: the tolerance handed to devsim is a step size control rather than an accuracy control, devsim refuses the tightest rung on this device at an inverted gate, and the looser rung it accepts returns a looser step. Two things bound what the floor lets through. The model bound: the most sensitive quantity any benchmark reads is the subthreshold drain current, exponential in the surface potential at the ideal 1/V_t, so 1e-5 V bounds the current it can move to exp(1e-5/0.02585) - 1 = 3.9e-4 relative, a decade under the smallest mesh error any of these files records and two under the 10 percent the comparison spends. The structural bound: accepting the floor does not end the solve, it marks the potential arrived and hands the rest to the terminal balance criterion, which is what certifies the current the benchmark reads, and whatever that reaches is written per point into the golden file and spent as tolerance. Anything above the floor still raises, and every accepted floor is printed |
| 2026-09-15 | Benchmark 10's degenerate ohmic contacts evaluate the mass action product at the Boltzmann majority density | Neutrality is untouched by the statistics, n - p = N, but mass action is not: with each density carrying its own degeneracy factor the product is n_i^2 gamma_n gamma_p. The majority carrier is still the quadratic root and the minority still comes from the product, so mass action stays exact rather than merely close, and the only approximation left is which density the two gamma factors are read at. The correction to the root is 4 n_i^2 against N^2, which at 1e20 is 1e20 against 1e40, so the two agree to forty digits. What is not dropped is the potential: inverting n = n_i exp((psi - V_t gamma_n)/V_t) puts the offset at 1.18 V_t at 1e20, which is 30.6 mV of built in potential at the source and the drain, and dropping that would move every threshold on the sweep |
| 2026-09-15 | The per iteration callbacks on all three solve/ loops receive scalars only, never the iterate, the state or the residual vector | Phase 7 needs a browser to watch the residual fall while a solve runs, and the acceptance criterion for that hook is that telemetry off leaves every solved number bit for bit unchanged. A frame holding a view of `x` or of `system.residual` would let a callback write into the arrays the next step is assembled from, so inertness would rest on every future callback author behaving. Handing over five scalars makes it structural instead: there is nothing in a frame to mutate. The cost is that per equation family telemetry cannot come from this module, which is correct anyway, because the split into families lives in the caller's `residual_norm` and `solve/` is not allowed to know what an equation family is. Field frames, which Phase 7 also wants, therefore come from the API layer reading the finished iterate, not from this hook. An exception raised inside the callback is deliberately not caught: that is the cancel path, and swallowing it would make a cancel button do nothing |
| 2026-09-15 | Cancelling a running job works by raising inside the telemetry callback, not by a flag threaded through the solver | A solve is a tight numerical loop that never looks up from the arithmetic except to report an iteration, so the report is the only place a stop can be noticed without adding a parameter to every signature between the browser and `newton_solve`. The three loops already refuse to catch exceptions raised in their callbacks, which makes the cancel path the same code as the no cancel path: nothing in the solver tests for cancellation, so cancellation cannot change a converged answer. The limit this buys is real and stated rather than hidden: a solve that has stopped reporting cannot be cancelled until it reports again, so a single very long Newton step is uninterruptible, and `api/jobs.py` says so in its module docstring |
| 2026-09-15 | `api/devices.py` reads the device constructors' own signatures rather than declaring a schema of its own | phases/PHASE-7.md asks for device definition in the browser with nothing hardcoded per device, and two copies of a parameter list is exactly how a browser ends up simulating something the docstring does not describe. Reading `inspect.signature` means a knob added to `nmos()` is offered the moment it exists and the default the browser shows is the default this project validated. The cost is a rule about annotations: only parameters annotated float, int or bool are offered, read as written text rather than evaluated, so a device constructor that grows a parameter of any other type will silently not be offered until this file learns that type. `material` is the one deliberate exclusion and it is refused in both directions, because a device built on a material the browser invented is not a device this project ever validated |
| 2026-09-16 | Telemetry reaches the public sweeps through one optional `on_frame` argument threaded down the call chain, not through three separate callbacks and not through a contextvar | The browser does not call a solver, it calls `iv_sweep`, `gate_sweep` or `cv_sweep`, and the three hooks added on 2026-09-15 sit two to four layers below those. Eleven signatures now carry the argument: `solve_poisson`, `solve_equilibrium`, `solve_bias`, `solve_bias_newton`, `solve_bias_ramped`, `solve_bias_hybrid`, `_gummel_prelude`, `_walk_sweep`, `iv_sweep`, `gate_sweep` and `cv_sweep`. One pipe rather than three because the frame types are already distinguishable by their own type and a reader handed three callbacks has to implement three. A contextvar would have needed no signature change at all and was rejected for exactly that: CLAUDE.md asks for explicit over clever, and hidden wiring in the one place where the inertness claim has to stay checkable is the wrong trade. |
| 2026-09-16 | The equilibrium Poisson solve that builds a cold bias solve's initial guess reports nothing, while every other solve inside a bias point does | `initial_state` runs the Phase 1 one unknown per node solve, whose residual shares no scale with the coupled residual plotted beside it. Reporting it would put a second unrelated trace on the residual plot with no way to tell which was which. The low field prelude and the surface fixed point sweeps do report, because their iterations are counted in the NewtonResult the caller is judged on, so a plot without them would be missing the expensive part of the solve. |
| 2026-09-16 | A sweep point's telemetry frame carries scalars only, so `IVPoint` and `CVPoint` are not themselves frames | The two new frame types `IVFrame` and `CVFrame` duplicate three fields each rather than reusing the point that already exists. The point carries the converged `DeviceState`, and on an I-V sweep that state is the guess the next point continues from: a frame carrying it would hand a reader the ability to change a solve that has not happened yet. That is the same structural argument `NewtonIteration` makes for itself, and it is the reason the inertness claim is checkable rather than a promise. |
| 2026-09-16 | Non finite numbers cross the socket as null | JSON has no infinity and no NaN. `json.dumps` writes both as bare words that `JSON.parse` refuses, so an encoder that passed them through would break the stream on the one frame a reader is watching for: `GummelIteration.update` is not finite by construction when the iteration has diverged. |
| 2026-09-16 | Field arrays travel over HTTP as `GET /api/jobs/{id}/fields/{index}`, not over the telemetry socket | phases/PHASE-7.md item 6 asks for field frames on completion and on request while running, with the client asking for a cadence and the server dropping frames rather than making the solver wait. A request with an answer is a GET: it works after the socket has closed, it cannot stall a solve by construction rather than by a dropping rule, and it removes the need for the socket to carry traffic in both directions. What is given up is a field arriving unasked while the solve runs, which nothing in the acceptance criteria needs. |
| 2026-09-16 | The API hands the browser `TransportModels.for_device`'s own defaults to render rather than choosing models for it | for_device defaults to constant mobility with no field dependence and no surface scattering, because that is what every result before Phase 5 was taken with. A MOSFET solved on those has no velocity saturation in it. Inventing a second set of defaults for the browser would put the project's validated physics in two places, so instead the flags are read off for_device's signature, shown on the form with those defaults visible, and whoever presses solve chooses. `MOBILITY_MODELS` was lifted out of `_scaled_diffusivity`'s error message into a module constant for the same reason: so the list the browser offers is the list that exists. |
| 2026-09-16 | `POST /api/jobs` validates everything it can without solving, including the terminal names, which duplicates a check the sweeps already make | A refusal the caller can answer beats a job that starts and dies with the same message somewhere less visible. The copy in `api/sweeps.check_request` only has to be no stricter than the sweep's own, because the sweep still checks and remains authoritative. |
| 2026-09-16 | Band edges on the band diagram come from the anchored n_i, so Ec - Ev there is kT ln(Nc Nv / n_i^2) = 1.12789 eV rather than Eg(300) = 1.12412 eV, 3.77 meV wide | `extract/bands.py` places Ec and Ev at kT ln(Nc / n_i) and kT ln(Nv / n_i) from the intrinsic level, which are the Boltzmann relations the solver actually uses. Drawing them Eg / 2 either side instead would put the bands out of step with the carrier densities on the same screen by a couple of meV. The 3.77 meV is the same anchoring of n_i at 1e10 that the n_i deviation below records, seen as an energy. Cited by `tests/unit/test_bands.py`. |
| 2026-09-17 | The client traces current streamlines through server computed (Jx, Jy) and interpolates server computed band edges along a cutline | phases/PHASE-7.md forbids physics in the client. Neither of these is physics: every vector and every energy was computed on the server, and what the browser does is geometry on arrays it was handed, bilinear interpolation between nodes and midpoint steps along a direction, the same argument that lets the log axis take a logarithm. The one numerical method this puts in the client is bounded by `tests/unit/test_client.py`, which refuses `Math.exp`, `Math.log`, `Math.pow` and `Math.sinh` in `streamlines.js` on top of the guards that already cover every first party script. |
| 2026-09-17 | `current_densities` reads each carrier's current with its effective potential inside the Bernoulli argument, the one the solve used, rather than with psi | The row of 2026-09-01 puts degeneracy into transport there, and the solve has done so since, but the extraction kept plain psi. On a Boltzmann device the two are the same array, so nothing measured before moves. On a degenerate one the drift and diffusion terms stopped cancelling: a 1e17 / 1e20 junction at 0 V reported 5.8e7 A/cm^2, and the coarse NMOS at Vg = 1 V reported 2.7e8 against a correct 1.0e5, which drew the first streamlines out of the source and drain into the bulk. Read with the effective potential the junction at rest reports 4.0e-6 A/cm^2, the cancellation floor of edge terms near 1e9. No solved number moves: terminal currents come from the continuity residuals, and the only production caller is `node_current_density`, which exists for the streamlines. Cited by `tests/invariant/test_node_currents.py`. |
| 2026-09-17 | The page offers a coarse mesh on mos_cap and nmos, off by default, and the diode gets none | A 2D solve takes seconds to minutes, and phases/PHASE-7.md Stage 2 asks for a mesh that answers in seconds. The coarse numbers are knowingly off the validated mesh, measured the same day: mos_cap at 129 nodes against 375 reads C 0.556 percent high in accumulation and 0.714 percent high in depletion over a -2 V to 2 V C-V. nmos at 1504 nodes against 8379 reads drain current 0.621 percent high at 1.2 V and threshold 0.7 mV off on a 50 mV drain transfer, in 5.1 s against 20.4 s. The page prints these next to the choice, and nothing in the README or the tier 4 benchmarks uses them. The diode solves a seven point I-V in 0.2 s on its converged mesh, so a coarse one would save nothing |
| 2026-09-18 | Each guided lesson's claims are tests on the lesson's own requests, held to the textbook with measured tolerances, and the two MOSFET lessons run on the coarse mesh | phases/PHASE-7.md Stage 3 asks for every claim a lesson makes to be a pytest check on the lesson's own device. `tests/analytic/test_lesson_claims.py` reads the lesson files and runs the exact request the page sends. Where a claim compares with a textbook formula the solver knowingly misses it, and the direction is asserted along with the size: the one-sided 1e18 / 1e16 junction keeps 95.6 percent of V_bi on the light side against the 99 the depletion approximation gives, because the heavy side's 4 nm depleted layer is one Debye length and holds 37 mV of tail (bar 95); the high frequency C-V minimum reads 0.085 C_ox against 0.0898 (below, within 8 percent); the 1 um nmos subthreshold slope reads 101 mV/dec against the 93.9 body factor estimate (above, within 15 percent) and its extrapolated threshold 0.847 V against 0.818 (within 50 mV). The short channel lesson runs `SHORT_CHANNEL_PROCESS` on the coarse nmos preset, measured against the converged mesh the same day: constant current thresholds 275, 226 and 93 mV at 1 um, 100 nm and 50 nm against 277, 229 and 95, and DIBL 9.1, 30.8 and 125.8 mV/V against 8.5, 30.7 and 126.7, at 15 to 20 s a curve against 47 to 74 s. The lesson text states these numbers. Every lesson was broken on purpose (the heavy doping undone, the fast response switched back, a reverse sweep sent forward, the 50 nm step drawn at 1 um, its drain left low, half the oxide on the 1 um nmos) and a claim failed each time |
| 2026-09-18 | The 1D stack builder takes regions of 1e14 to 1e19 cm^-3 only, grades one mesh to every junction with a shared growth rate, and refuses a region holding fewer than two mesh nodes inside it | phases/PHASE-7.md Stage 4 asks for refusals at the doping range docs/01-physics.md states for the models, and that doc stated none, so the range is now written there with its measurement: at 1e19 Boltzmann puts the Fermi level 3.2 mV off Fermi-Dirac and the Einstein ratio at 1.122, at 3e19 9.5 mV and 1.361. The bottom end is the pn diode knobs' own, where mobility and lifetime have already flattened. Grading each junction over its own segment and joining halfway was tried first and failed: each segment's growth is set by its own longest side, so a 50 nm base joined its neighbours with jumps up to 2.7 at five of the six h_min and n_nodes pairs tried, and the refusal advised adding nodes that did not help. `graded_mesh_1d_at` solves one growth rate g for the whole mesh from h = h_min + g d, so the same stacks grade at 1.02 to 1.31 and get gentler as nodes are added, held to the 1.5 graded_mesh_1d holds. With one junction it is graded_mesh_1d itself, so the Phase 2 diode drawn as a stack reproduces pn_diode's I-V bit for bit and no tolerance is recorded. Two nodes inside a region make three cells, the least that shows a slope inside it rather than a straight line between its boundaries. A heuristic, not a convergence criterion: a student's structure is labelled on the page as unvalidated either way |
| 2026-09-18 | A 2D device can be drawn from rectangles: blocks of silicon and oxide painted in order, implants added up, electrodes on straight segments. Every edge drawn is a mesh line, each axis graded with one growth rate to every doping edge and every Si/SiO2 interface. An implant edge flush with the device boundary is open, the implant carrying on past it. A Gaussian implant is the window blurred by an erfc of `lateral` across and a Gaussian tail of `straggle` up and down. Doping runs 1e14 to 1e19 cm^-3, or to 1e20 with Fermi-Dirac on. A mesh over 20000 nodes is refused. Surface mobility is refused on a device with a vertical Si/SiO2 wall | phases/PHASE-7.md Stage 5. The implant shape is the one nmos is built from, so the drawn benchmark nmos has nmos's doping bit for bit on the source side and to 1e-14 of the terms on the drain side, tests/unit/test_drawing.py. Grading every span on its own and joining them is what broke the 1D thin base in Stage 4, so one growth rate again. 1e20 is the nmos source peak, n/Nc = 3.5, inside the Joyce-Dixon range docs/01-physics.md uses. The budget is the benchmark nmos at 8379 nodes and 19 s for a transfer curve, measured the same day, with room for a finer mesh. normal_field reads dpsi/dy, which is the normal to a flat interface only; on a side wall Lombardi would scatter off the wrong field and nothing would show it |
| 2026-09-19 | A drawn device is refused when a p or n region of its silicon has no ohmic contact on it, not only when a whole silicon island has none. So an SOI film needs a body tie, and a floating body is not simulated | A p film on buried oxide with n+ source and drain, contacted at both, stalled at 25 uV of drain and never met the 1e-10 criterion. I measured why on 2026-09-19. The Jacobian agrees with complex step on every edge whose drop is above 1e-5, so the derivative is right. The Newton matrix is singular to about 2.5e17 after equilibration, and its near null vector is the potential of the whole p body along the BOX moving as one piece. The body's holes reach a contact only through junction leakage, which double precision cannot resolve next to the majority terms in the same rows. The same film with a body tie converges in 3 iterations, quadratically, from the same start. A rule taken from doping and geometry alone keeps this honest. Loosening the criterion would mark the body rows converged when they are not. A real floating body treatment, a body quasi-Fermi level as an unknown or an extended precision solve, is the other route and is not built. I have not measured why some bulk devices with no body contact did converge, and they are now refused too, since their p body floats the same way. It removes a class of device rather than getting one wrong: kink effects and floating body history cannot be drawn until the solver treats the body. tests/unit/test_drawing.py draws the stalled film and refuses it, and draws it with a tie and builds it |
| 2026-09-20 | A Newton iterate whose residual terms overflow is reported as a diverged solve, not raised, and an iv sweep whose cold start fails ramps its held biases in with solve_bias_ramped before giving up | I pushed every api knob to its extremes and every request shape through the real routes. Two held-bias cases failed as jobs instead of solving. On the MOSFET, a drain held at 10 V overflowed a Newton iterate, and the row-scale guard in residual_term_scales raised a ValueError meant for a state with no terms at all, so the cold-start ramp died before it could take a smaller step. Overflow now raises FloatingPointError, which newton_solve already treats as divergence, and the same device solves with the drain held at 10, 20 and 100 V. On the diode, the iv cold start is the equilibrium solve with the whole bias on, and it failed at 20 V reverse and 2 V forward. It now falls back to the coupled ramp and lets Gummel continue from there. Both changes are on branches that used to raise, so no result that converged before moves. A sweep that used to start cold starts exactly as it did. What changes is that requests which failed now produce a curve. tests/unit/test_newton.py, test_coupled.py and test_iv.py hold it, and the iv test checks the 20 V reverse current moves under 5 percent over a tenth of a volt, as generation current should |
| 2026-09-22 | The client is restyled to the design system in docs/08-design.md: a dark three pane bench, Archivo and Roboto Mono vendored alongside marked and KaTeX, and one palette shared by the chrome and the canvases. The four trace colours move from blue/green/red/purple to cyan/amber/coral/violet, the valence band gets its own colour instead of sharing the conduction band's, and the 2D field image drops its blue to yellow rainbow for a single hue ramp through the page ground to the signal cyan | Colour is the only thing here a reader could mistake for a result, so it is written down. Nothing numeric moves: the client computes no physical quantity and tests/unit/test_client.py still holds it to that, unchanged. Two of the three changes are legibility rather than taste. Ec and Ev were drawn in one ink and read as one curve. The rainbow ramp invented a visible boundary wherever its hue turned, which on a potential map looks like a junction that is not there; a sequential ramp has no such feature. The greys the design specifies for the dimmest two text tiers measured 3.74:1 and 2.75:1 against their own backgrounds, so both were lifted, to 4.80:1 for the one that carries text and 3.17:1 for the one that only draws a control. The page's ids are a contract with tests/unit/test_browser_smoke.py and none were renamed, which is why all 16 of its tests pass unchanged; the one test that had copied the old colour ramp into Python now asks the page for its own, since what it checks is which node lands under which pixel |
| 2026-09-22 | The drawing preview stretches its y axis piecewise rather than linearly: every band between two drawn edges gets at least 30 px of canvas and the rest is shared out in proportion. The picture says "y stretched" under itself whenever it does this, and states the real extents beside it. Doped rectangles are also washed in their dopant's colour rather than only outlined | A 2 nm gate oxide on a 1 um body is 0.2 percent of the height. Drawn to scale it is a hairline under the labels and the device reads as a featureless slab, which is what the editor looked like before this. The stretch is a statement about a screen and not about the device: the rows still hold the real numbers, the key states the real extents, the request is built from the rows, and back() is the exact inverse of the mapping so a drag lands where it looks like it landed. Two browser tests drag on that canvas and both pass unchanged, which is what says the inverse is right. Nothing physical is computed to do it: the band edges are the y values already typed into the rows | 
| 2026-09-22 | The client is set in one family, Schibsted Grotesk, and the monospace named in the row above is gone. Numbers hold their column through `font-variant-numeric: tabular-nums` on `body`, which the reading prose sets back to `normal` | Recorded because the row above names two fonts that are no longer in the tree, and a reader chasing it would otherwise be looking for files that were deleted. The monospace was there for one property, that a value under a live slider does not reshuffle the row it sits in, and tabular figures give that property inside a proportional face. Nothing numeric moves: no test reads a typeface, and the 32 client tests pass unchanged. The vendored fonts went from twelve files to four |
| 2026-09-22 | The client is set in Poppins at 500 and 600, replacing the family in the row above. Poppins has no tabular figure feature and its digits are proportional, so the `font-variant-numeric` that row relied on is gone and the numbers are held still by geometry instead: every value sits in a fixed width box, and `#state` carries a `min-width` | Asked for by name. Recorded because the reasoning in the row above no longer holds and somebody reading it would look for a property this font does not have. Measured rather than assumed: the digit advances in Poppins-Medium run 350 to 647 units and neither weight lists `tnum` in its GSUB. The knob rail went from 312 px to 340 px because Poppins sets wider and "Convergence tolerance" no longer fit beside its box. Nothing numeric moves and no test reads a typeface; the 32 client tests pass unchanged |

## Known deviations from reference

Things that do not match DEVSIM or an analytic result, with the reason, so they
are not rediscovered as bugs later.

| Item | Deviation | Reason | Acceptable? |
|---|---|---|---|
| The frontend and `log` | phases/PHASE-7.md asks that grepping the client for `exp`, `log` and any constant in docs/06-constants.md finds nothing. The client contains `Math.log10` and `Math.pow(10, ...)`, once each | Item 7 of the same phase asks for log scale toggles, and a log axis cannot be drawn without a logarithm. Mapping a decade to a row of pixels is arithmetic about a screen rather than about silicon. The relaxation is bounded and tested: tests/unit/test_client.py requires exactly one `Math.log10` and one `Math.pow`, both inside the two named axis helpers, refuses `Math.log`, `Math.log2`, `Math.exp` and the other transcendental functions outright, and refuses every public name in ddsim/core/constants.py | Yes, and checked rather than asserted |
| ddsim drain terminal current at deep off bias | ddsim floors near 2e-9 A/cm and can sit negative there; DEVSIM resolves the same region cleanly, every point positive and strictly rising down to 2.4e-9 on the 200 nm at 50 mV of drain | The drain contact sits in 1e20 doping, so the terminal current is a sum of Scharfetter-Gummel fluxes each far larger than their difference, and at deep off bias the true current falls under the cancellation roundoff. DEVSIM takes its contact current from the residual of the continuity equation at the contact nodes instead, which does not difference two large fluxes. Nothing in Phase 5 reads a current there: benchmark 9 extracts at 5e-3 to 2e-2 A/cm, six to seven decades above the floor | Yes for Phase 5. Benchmark 9 trims the affected points and bounds the trim, see the 2026-09-13 row above. It would matter to any off state leakage or subthreshold slope claim taken below about 1e-8 A/cm, and nothing yet makes one. Revisit if a leakage figure is ever a deliverable |
| n_i vs Nc, Nv, Eg | `sqrt(Nc*Nv)*exp(-Eg/(2*V_T))` = 1.0757e10, we use 1.0e10. 7.6 percent in n_i, 16 percent in n_i^2 | n_i is anchored to the textbook value by decision. Nc and Nv are the measured 300 K values. They are not mutually consistent and cannot both be primary | Yes for now. It will matter the first time something computes a band edge position or a Fermi level from Nc. Revisit at Phase 2 |
| Intrinsic Debye length | docs/06-constants.md says "roughly 24 um". The doc's own formula with the doc's own n_i gives **40.885 um** | 24 um reproduces exactly as `sqrt(eps*V_T/(2*q*1.45e10))` = 24.01 um, so that line carries both an extra factor of 2 and the superseded 1.45e10 n_i. The doping table in the same section (128, 40, 13, 4.1, 0.4 nm) is consistent with the formula, so only the intrinsic line is wrong | Corrected in the doc on 2026-08-21. Code follows the formula, and a test pins 40.885 um so a revert cannot take the code with it |
| Built-in potential, 1e16/1e16 | docs/06-constants.md says "about 0.695 V". With n_i = 1.0e10 it is **0.7143 V** | 0.695 V is the n_i = 1.45e10 answer. Same superseded constant as the line above | Corrected in the doc on 2026-08-21, and in phases/PHASE-1.md. This one bites in Phase 1, where V_bi is an acceptance target and a 19 mV offset reads exactly like a boundary condition sign error, so a test asserts 0.7143 and separately asserts it is not 0.695 |
| Symbolic factorization reuse | docs/02-numerics.md calls it a "significant speedup for free". It is not available at all | scipy's splu exposes no symbolic and numeric split. See the Broke entry | Corrected in the doc on 2026-08-21, with the measurement that killed it. The interface is built so a UMFPACK or KLU backend can deliver it later |
| V_bi in the Phase 1 acceptance criteria | phases/PHASE-1.md asks for V_T*ln(Na*Nd/n_i^2) to under 0.5 percent and then says "expect about 0.695 V". Those disagree by 2.8 percent, five times the tolerance | The same superseded n_i = 1.45e10 as the constants doc. The formula gives 0.7143 V with our n_i | The formula wins. A test asserts 0.7143 V and asserts it is not 0.695, so the discrepancy stays visible |
| devsim's own bias ramp on a MOSFET | `devsim.python_packages.ramp.rampbias` calls `solve` once per bias step and trusts what it reports. Run that way on the 1 um NMOS it reports success at every step and hands back a transfer curve reading **-2.209939 A/cm at 0.1 V of gate**, +9.942419 at 0.4 and -2.783957 at 0.6 | A devsim `solve` returns after a single damped step and reports its relative and absolute update as exactly zero, which is a statement about the size of that step and not about the residual. The generator's `settle` decides convergence itself, by calling solve until the potential stops moving to 1e-9 V and then checking the state is physically possible at all. Same equations, same reference, driven differently | Yes, and the generator would be wrong without it. Every current it writes is cross checked against the drain and source cancelling, which they do to between 2e-5 and 7e-9 |
| Depletion width on a heavily doped side | x_p/x_n = Nd/Na is only recovered when the depleted width exceeds the local Debye length | At 100 to 1 doping the heavy side depletes over 0.74 Debye lengths, so it is entirely smeared and has no abrupt edge to find. Measured ratio 29 against a predicted 100 | Yes. This is the depletion approximation failing, not the solver. Tested at 10 to 1 where it holds to 30 percent |
| Reverse bias without continuation | -10 V needs 46 Newton iterations against a budget of 50 | The step limiter caps psi at 5 V_T per step, so a 10 V shift needs many of them | Acceptable for now. Bias continuation in Phase 3 is the proper fix, not a bigger budget |
| Current continuity below 0.25 V forward | phases/PHASE-2.md asks for Jn + Jp constant to 1e-6 at any bias. Measured: 3.3e-10 at 0.5 V, 1.3e-8 at 0.4 V, 2.8e-7 at 0.3 V, 1.5e-5 at 0.2 V, 6.6e-4 at 0.1 V | Not a conservation error. Jn is the difference of two edge terms of size (Dn/h)*n, and near equilibrium they cancel to nothing, so the relative spread is machine epsilon times the ratio of a flux term to the current. Measured spread over eps times that ratio sits between 0.4 and 1.2 across seven decades of bias, which is what a subtraction out of digits looks like and is not what a broken scheme looks like | Yes, and tested as such. The gate is asserted at 0.3 V and above, and the cancellation model is asserted separately at every bias including reverse |
| Terminal current sum below 0.25 V forward | Asked for 1e-8 relative. Holds from 0.3 V up (5.6e-9, 2.6e-10, 2.0e-12). At -1 V it is 2.5e-5 relative | The leftover is the sum of the interior residuals, and those are edge flux differences with the same cancellation. In absolute terms it is 1e-13 A/cm^2 against an arithmetic floor of 6.3e-13 | Yes. The strict 1e-8 test runs at forward bias and a separate test bounds the low bias case against the floor rather than against a relative tolerance |
| Where Gummel gives up | phases/PHASE-2.md expects degradation above roughly 0.6 V and asks for the bias at which it fails. It does not fail. Continued in 0.05 V steps it takes 4 cycles at 0.3 V, 12 at 0.7 V, 25 at 0.9 V, 60 at 1.1 V, and the per cycle convergence rate climbs from 0.51 at 0.9 V to 0.95 at 1.8 V | The degradation is exactly as predicted, it is just graceful. Two things help: the Poisson block is solved nonlinearly at fixed quasi-Fermi levels rather than with frozen densities, and continuation hands each solve a good guess. Where a solve does exceed its budget the continuation driver halves the step and the retry succeeds | Yes, and better than the phase expected. The rate curve is the honest answer to the question and it is what motivates Phase 3 |
| Peak ideality factor | 1.79 rather than 2.0, at 0.16 V on the 1e18 device | Two reasons, both physical. The depletion region narrows under forward bias, so the recombination volume shrinks and the current rises slightly faster than exp(V/2V_T), which pulls the apparent ideality below 2. And diffusion current still contributes a few percent at the peak. A sum of two mechanisms has an apparent ideality strictly between theirs | Yes. 2.0 is the limit of a single idealized mechanism, and a solver that reported exactly 2 would be reporting the formula rather than the device |
| Boltzmann statistics at 1e18 | The ideality crossover device runs at 1e18, where n/Nc is 0.035 and Joyce-Dixon puts the Fermi level correction at about 0.3 mV | docs/01-physics.md defers Fermi-Dirac to Phase 5. The correction shifts I_s slightly and leaves a slope alone, and the ideality is a slope | Yes for this use. It would not be acceptable for a quantitative I_s claim at that doping, and no such claim is made |
| "Gummel failed at 1.0 V" | phases/PHASE-3.md makes converging at 1.0 V "where Gummel failed" the headline criterion of the phase. Gummel does not fail at 1.0 V, and it does not fail at 2.0 V either | Measured on the 1e16 diode, warm started along 0.05 V continuation steps with a 2000 cycle budget: Gummel takes 3 cycles at 0.1 V, 6 at 0.6 V, 46 at 1.0 V, 101 at 1.2 V, 224 at 1.5 V and 466 at 2.0 V, converging every time. Newton takes 4 to 6 across that whole range and does not grow. An earlier reading that Gummel stalled at 1.63 V was the 200 cycle default budget in solve_bias, not a divergence | The criterion is restated rather than dropped. What is tested is that Newton reaches 1.0 V in under a third of the cycles and reaches it cold. The real answer to the phase's question is that Gummel degrades without bound instead of failing, and by 2.0 V it costs 116 times more |
| Cold start above roughly 1.3 V | initial_state, which is the Phase 1 Poisson solve at frozen quasi-Fermi levels, does not converge on a 1e16 diode above about 1.3 V, or on a coarse 1e15 diode above about 1.1 V. It raises rather than returning | The limit is in the guess, not in the coupled Newton. Newton converges at 1.4 V on a 1e18 device and at 2.0 V on a 1e16 one whenever it is handed any state to start from | Yes, and it is what docs/05-pitfalls.md already prescribes: continue from equilibrium, always. Continuation reaches 2.0 V in 8 solves with no retries. Where there is no ramp to come up, the Gummel prelude covers it |
| Asymmetric junction at 1e20 / 1e14 | Was: the electron continuity family stalled at a scaled residual of 2.8e-9 against a threshold of 1.0e-10. **Fixed on 2026-08-22.** | The diagnosis in the row this replaces was wrong. It blamed a single term scale spanning six decades of doping and proposed per node row scaling. The actual cause was that the scale was measured once at the starting guess, and the electron term scale on this device grows by 660000 between equilibrium and 1 V because the minority density on the 1e20 side is injected up by exp(V/V_T). The solve was converged to 4.5e-15 against the terms it actually had, and the threshold was 660000 times too strict | Fixed, and the device converges at 1 V in 14 steps. Boltzmann statistics are still invalid at 1e20 and no quantitative claim is made there; what is claimed is that the solver reports convergence honestly. A test pins it |
| Arora against the tabulated undoped mobility | docs/06-constants.md lists mu_n = 1417 and mu_p = 470 for undoped silicon, and separately gives Arora parameters whose N -> 0 limit is 88 + 1252 = 1340 and 54.3 + 407 = 461.3. 5.4 percent apart for electrons, 1.8 for holes | Both are measured, from different fits to different data. Arora is fitted over the doped range where it gets used rather than at an intrinsic limit nobody measures | Yes, and the reason the constant model stays the default. Switching a device to Arora moves its current by five percent even at doping low enough that the model should be doing nothing, and that would otherwise read as a bug. A test pins both numbers |
| Boltzmann statistics at 1e20, in the MOSFET source and drain | n/Nc at 1e20 is 3.50 against Nc = 2.86e19, well past degenerate. The Joyce-Dixon series puts the Fermi level correction at 30.5 mV there, against 3.2 mV at 1e19 | phases/PHASE-5.md scope item 2 asks for Fermi-Dirac by Joyce-Dixon and it is the next stage. The equilibrium geometry had to exist before there was anything to apply it to | Resolved on 2026-09-09. The NMOS is solved with Fermi-Dirac by Joyce-Dixon, and the built in potential across the source junction moved by the 30.5 mV predicted here. The test that asserts it now reads its closed form from the device's own statistics rather than from asinh, and pins the 30.5 mV separately, so a revert to Boltzmann fails it twice. Every device that does not set the flag is unchanged, including the five DEVSIM benchmarks |
| Accumulation approaching C_ox | The C-V curve is 1.7 percent below C_ox at V_FB - 2.6 V and 5 percent below at V_FB - 1 V. It reaches 1 percent only by about V_FB - 5 V | The accumulation layer has a finite thickness, so the stack is C_ox in series with a large but not infinite semiconductor capacitance. A thinner oxide makes this worse, not better, because C_ox is the larger of the two | Yes, and it is physics rather than a defect. phases/PHASE-4.md gates accumulation at 1 percent without naming a bias, so the figure and the tests state the bias they read it at |
| Joyce-Dixon second coefficient in docs/01-physics.md | The doc wrote the series as `ln(n/Nc) + (1/sqrt(8))(n/Nc) - (3/16 - sqrt(3)/9)(n/Nc)^2`. 3/16 - sqrt(3)/9 is itself negative, -4.95009e-3, so the leading minus sign flips it to +4.95009e-3 | A transcription slip rather than a different convention: the published A2 is -4.95009e-3 and enters with a plus sign. Measured against Brent inversion of the integral, the series as the doc wrote it lands 1.6e-1 from the true eta at u = 4, against 1.0e-4 for the published sign, three decades worse, and the error grows with density in the direction that matters | Corrected in the doc on 2026-09-01. The code carries the published sign and a test pins A2 against both its closed form and its published decimal, so a revert cannot take the code with it |
| SRH equilibrium product under Fermi-Dirac | SRH takes its equilibrium product from n_i^2. Under Fermi-Dirac the equilibrium product is n_i^2 gamma_n gamma_p, which on the 1e20 side of a 1e17 / 1e20 junction is 0.307 of n_i^2, so the model sees np - n_i^2 = -0.693 at rest and generates where nothing should be happening. Equilibrium is therefore not an exact fixed point of the degenerate continuity equations: the leftover residual is 3.1e-5 of the flux terms of the rows that carry it | Not noticed until the residual was measured row by row, because the family wide scale divided it by a number set on the degenerate side and reported 1e-15. The term is exactly accounted for rather than estimated: subtracting R*volume from both continuity families leaves 1.4e-14, which is roundoff, and a test asserts that rather than a loose threshold | Yes for now, and sized rather than assumed. The spurious rate peaks at 4.7e8 cm^-3 s^-1 and integrates to 3.7e-15 A/cm^2, against 8.9e-9 A/cm^2 of real forward current at 0.1 V on the same device, so it is six decades below anything this project claims and seven below the off state drain current of the coarse MOSFET mesh. It will matter the first time a quantitative claim is made about generation in degenerate material, and the fix is to give SRH the equilibrium product of the active statistics rather than n_i^2. Its own change, with its own test, because it moves the recombination in every degenerate source and drain |
| Drawn benchmark devices against the constructors | The drawn MOS capacitor reproduces mos_cap C-V to 8.7e-5 relative at worst over -2 to 2 V, asserted at 2e-4. The drawn nmos reproduces nmos Id-Vg at 50 mV drain to 1.01e-2 at worst where the drain current exceeds 1e-7 A/cm, asserted at 1.5e-2, and below that neither is compared to the other | The doping is the same function; the mesh is not, because a drawing grades each axis through every edge drawn where the constructors lay hand built segments. Measured 2026-09-18, mos_cap moves 1.8e-3 when refined to n_silicon=241, n_oxide=9, h_min=2.5e-8, and nmos moves up to 1.6e-2 when refined to h_min_y=3.125e-9 with n_oxide=65, so both differences sit at or under the mesh error of the device they are compared with. Under 1e-7 A/cm the drain terminal floor near 2e-9 A/cm dominates: at 0 and 0.1 V nmos and nmos refined read inside it with opposite signs | Yes. A mesh difference, and tests/analytic/test_drawn_devices.py checks both it and that the drawing is off wherever nmos is. A mutant with the lateral spread dropped moves the current by 23 percent and fails |
